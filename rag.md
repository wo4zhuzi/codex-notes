# RAG 使用笔记

更新时间：2026-05-25。

本文记录 RAG 的核心概念、项目落地方式和一个最小 demo。本文重点解释工程链路，不把 RAG 等同于向量数据库或 embedding。

## 结论

RAG 是 Retrieval-Augmented Generation，中文常译为检索增强生成。

在项目中，RAG 的本质是：

```text
用户提问
-> 系统先检索自己的资料
-> 把相关资料交给模型
-> 模型基于资料回答
```

所以 RAG 不是一个模型，也不是单个数据库，而是一条应用链路。

## 为什么需要 RAG

只让模型直接回答时，模型主要依赖训练数据和当前上下文。它可能不知道你的内部文档、最新规则、项目配置、业务数据和私有知识。

RAG 解决的是这个问题：

```text
模型会表达和总结
RAG 负责把正确资料找出来
```

适合使用 RAG 的场景：

- 企业知识库问答。
- Markdown / PDF / Wiki 文档问答。
- 客服政策问答。
- 代码库文档助手。
- 合同、制度、流程、售前材料总结。
- 需要引用来源的专业问答。

## RAG 在项目中的位置

典型结构：

```text
用户
-> Web / CLI / API
-> RAG Service
   -> Retriever 检索文档
   -> Prompt Builder 注入上下文
   -> LLM 生成答案
-> 返回答案和来源
```

如果加入 Agent，结构会变成：

```text
用户
-> Agent 判断是否需要查资料
-> 调用 RAG 检索知识
-> 必要时调用函数、数据库或 MCP 工具
-> 汇总结果并回答
```

Embedding 在这里是底层检索能力，不是 RAG 的全部。

## 和其他概念的区别

| 概念 | 作用 |
| --- | --- |
| RAG | 先检索外部知识，再让模型基于知识回答。 |
| Embedding | 把文本转换成向量，用于语义相似度检索。 |
| 向量数据库 | 存储和查询 embedding 向量。 |
| Function Calling | 让模型生成结构化函数调用参数，由应用执行真实函数。 |
| MCP | 让 AI 客户端用统一协议访问外部工具和资源。 |
| Agent | 上层决策者，决定是否检索、调用工具、拆分任务或多轮执行。 |

Function Calling 和 RAG 可以组合。例如客服系统可以先通过函数查询订单，再用 RAG 检索售后政策，最后让模型生成答复。

MCP 也可以和 RAG 组合。例如通过 MCP 暴露文档检索、CodeGraph、数据库只读查询等能力，Agent 再按需调用。

## 最小 RAG

最小实现不一定需要 embedding。

对于小型 Markdown 知识库，可以先做：

```text
读取 Markdown
-> 关键词检索
-> 取 topK 文档片段
-> 拼进 prompt
-> 模型回答
```

这已经能体现 RAG 的核心：模型回答前先拿到外部资料。

本仓库 demo 位于：

```text
demos/rag-notes/
```

运行方式：

```bash
cd demos/rag-notes
export OPENAI_API_KEY="你的 API Key"
go run . "RAG 是什么？"
```

网页版本：

```bash
go run . -web
```

打开：

```text
http://127.0.0.1:8081
```

## 标准 RAG

当文档变多、用户问题更口语化时，可以升级为 embedding RAG：

```text
文档解析
-> 切块
-> 生成 embedding
-> 写入向量数据库
-> 用户问题生成 embedding
-> 向量检索 topK
-> 注入上下文
-> 模型回答
```

这种方式比关键词检索更擅长处理同义表达。例如用户问“怎么接入外部工具”，文档里写的是“配置 MCP server”，向量检索仍可能召回相关内容。

## Hybrid RAG

生产环境通常不会只靠向量检索，而是使用 Hybrid RAG：

```text
关键词检索
+ 向量检索
+ 元数据过滤
+ 结果融合
+ reranker 重排
+ LLM 回答
```

推荐默认组合：

```text
BM25 / 全文搜索 + embedding 向量检索 + RRF 融合 + 引用来源
```

原因：

- 关键词检索适合文件名、函数名、配置项、错误码和专有名词。
- 向量检索适合语义相近但字面不同的问题。
- 元数据过滤可以处理版本、权限、时间和文档类型。
- reranker 可以从候选片段里挑出最相关的上下文。

## 工程落地建议

按复杂度递进：

1. 小知识库：先做关键词检索 RAG，验证链路。
2. 文档增多：加入 embedding 和向量库。
3. 召回不稳定：加入关键词检索和 RRF 融合。
4. 上下文噪声多：加入 reranker。
5. 企业场景：加入权限过滤、版本过滤、审计日志和质量评估。

不要一开始就做完整生产级 RAG。先让一个最小闭环跑起来，再根据真实问题升级检索层。

## 常见误区

- 误区一：用了向量数据库就是 RAG。实际 RAG 的关键是“检索结果参与生成”。
- 误区二：RAG 必须用 embedding。小型文档库可以先用关键词检索。
- 误区三：RAG 可以保证答案正确。RAG 只能提供证据，仍需要检索质量、prompt 约束、引用和评估。
- 误区四：Agent 会替代 RAG。Agent 更像调度层，仍然需要 RAG 提供外部知识。

## 本仓库 demo 的边界

`demos/rag-notes/` 是教学 demo：

- 使用本地 Markdown 文件作为知识库。
- 使用关键词评分作为检索方式。
- 使用 OpenAI Responses API 生成回答。
- 返回答案和来源文件。

它不包含：

- embedding。
- 向量数据库。
- reranker。
- 权限系统。
- 生产级评估集。

这个边界是有意保留的。先理解 RAG 链路，再升级检索实现，学习成本最低。
