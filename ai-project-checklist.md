# AI 项目落地清单

本文用于新项目启动或现有项目接入 AI 协作时做检查。它不是团队强制规范，而是个人知识库中的可复制参考清单。

## 结论

一个适合 AI 协作的项目，优先保证三件事：

```text
导航清楚 -> 验证可执行 -> 变更可追溯
```

如果时间有限，先补 `AGENTS.md`、验证命令和 `docs/changes/`。没有机械验证的项目，AI 很难稳定交付。

## 启动前检查

- 是否已经运行 `/init`，并生成或更新项目级 `AGENTS.md`。
- `README.md` 是否说明项目用途、启动方式和核心目录。
- 是否明确常用命令：安装、启动、测试、lint、typecheck、build。
- 是否明确配置来源：环境变量、配置文件、示例配置和本地私有配置。
- 是否说明不能提交的内容：真实 API Key、Token、私有代理地址、内部服务 URL。

## AI 协作规则

- 低风险小改动可以使用 vibe coding，但仍要限制范围、验证和检查 diff。
- 日常开发默认使用 Plan 模式，执行前先说明目标、范围、步骤和验证方式。
- 新项目、高风险任务、跨模块重构、迁移、权限、支付、安全相关任务使用 Spec 模式。
- 核心业务逻辑、bugfix、重构、权限、安全、数据一致性优先考虑 TDD。
- 每次产生仓库文件改动后，生成或更新 `docs/changes/YYYY-MM-DD-<topic>.md`。

## 文档结构建议

最小结构：

```text
README.md
AGENTS.md
docs/changes/
```

复杂项目可继续补充：

```text
docs/specs/
docs/architecture.md
docs/adr/
docs/deploy.md
docs/runbook.md
```

文档分工：

| 文档 | 作用 |
| --- | --- |
| `AGENTS.md` | 给 AI 的仓库导航地图和硬规则 |
| `README.md` | 给人类的项目入口 |
| `docs/specs/` | 实现前的需求边界、方案和计划 |
| `docs/changes/` | 实现后的实际变更、验证和风险 |
| `docs/architecture.md` | 架构约束、模块关系和关键取舍 |
| `docs/runbook.md` | 运行、排障、发布和回滚步骤 |

## 验证命令

项目应尽量提供一条或几条可复制的验证命令。

常见项目参考：

| 项目类型 | 建议验证 |
| --- | --- |
| Node.js / 前端 | `npm test`、`npm run lint`、`npm run typecheck`、`npm run build` |
| Python | `pytest`、`ruff check .`、`mypy .` |
| Go | `go test ./...`、`go vet ./...` |
| Rust | `cargo test`、`cargo clippy`、`cargo fmt --check` |
| Java / Maven | `mvn test`、`mvn verify` |
| 文档仓库 | `markdownlint README.md *.md`、链接检查、敏感信息扫描 |

没有自动化测试时，至少写清楚手动验证步骤和残余风险。

## Git 工作流

- 开始任务前查看 `git status --short`，确认是否存在用户未提交改动。
- 修改前先规划影响范围，不覆盖无关文件。
- 修改后先运行验证，再检查 diff。
- 默认不自动 commit，除非用户明确授权。
- 需要提交时，提交信息保持短句、动宾结构，并只包含本次任务相关文件。
- 需要 PR 时，说明变更摘要、影响范围、验证结果、风险和未覆盖项。

## AI 权限边界

默认允许：

- 读取文件、检索代码、查看配置和文档。
- 运行非破坏性检查命令。
- 提出计划、方案、风险和验证方式。
- 在用户授权后修改任务范围内的文件。

需要用户确认：

- 删除文件或大规模移动文件。
- 执行 `git reset`、强制覆盖、清理工作区等破坏性操作。
- 安装依赖、联网下载、升级主要框架或修改锁文件。
- 数据库迁移、生产配置、发布脚本、CI/CD 权限相关改动。
- 提交 commit、创建 tag、push 远端或发起发布。

禁止：

- 写入真实 API Key、Token、密码、私有代理地址或内部服务 URL。
- 伪造测试结果、构建结果或外部文档来源。
- 在未说明风险的情况下扩大任务范围。

## 发布和回滚

个人项目可以保持轻量，但生产项目应补充：

- 发布前验证命令。
- 数据迁移步骤和回滚方式。
- 配置变更清单。
- 日志、指标和告警观察点。
- 回滚触发条件。

AI 可以协助整理发布和回滚方案，但生产执行需要人类确认。

## 新项目推荐顺序

1. 运行 `/init`，生成项目级 `AGENTS.md`。
2. 补 `README.md` 的启动、验证和目录说明。
3. 写清楚验证命令。
4. 决定是否需要 Spec 模式。
5. 对核心逻辑决定是否使用 TDD。
6. 建立 `docs/changes/` 变更记录习惯。
7. 需要团队协作时，再补 CI、PR 模板、发布和回滚文档。
