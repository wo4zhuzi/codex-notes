# Codex Agent 运行机制

本文记录 Codex 收到一句话后，如何加载规则、发现上下文、选择工具、读取文件、执行命令、验证结果和沉淀状态。

它不替代 [LLM 与 Agent 原理地图](./llm-agent-principles.md) 或 [Agent 上下文管理](./context-management.md)。前者解释通用原理，后者说明如何准备上下文；本文聚焦 Codex 这个具体 Agent runtime 的运行链路。

## 核心结论

Codex 不会默认读取完整仓库，也不会因为某个普通 Markdown 文件存在，就自动把它当成任务状态。

更准确的链路是：

```text
用户输入
-> 加载系统、开发者、AGENTS 和当前会话指令
-> 根据任务判断需要哪些上下文
-> 选择性调用搜索、读文件、命令、MCP 或其他工具
-> 把工具观察结果放回会话
-> 继续计划、执行、验证或停止
```

所以，“Agent 知道什么”不是由仓库里有什么文件直接决定，而是由以下几类信息共同决定：

- 已经进入模型上下文的指令、对话和工具结果。
- Codex runtime 自动发现的规则和可用能力。
- 用户明确提供或提到的文件、命令、日志和目标。
- Agent 通过工具探索后读取到的仓库事实。

## Codex 一开始能看到什么

Codex 启动或开始任务时，通常会先拥有几类显式上下文。

### 系统和开发者指令

这类指令由运行环境提供，优先级最高，用来约束安全、工具使用、输出语言、协作模式、沙箱权限和最终回复要求。

用户通常看不到完整内容，但它会影响 Codex 能不能改文件、能不能联网、什么时候需要请求授权、最终回复要包含哪些信息。

### `AGENTS.md`

`AGENTS.md` 是稳定的仓库级规则入口。Codex 会按全局、项目、子目录的顺序发现并加载指令文件；离当前工作目录更近的规则会出现在后面，因此可以覆盖更上层的通用规则。

这类文件适合写：

- 项目结构和关键入口。
- 常用构建、测试、检查命令。
- Git、提交、push 和变更记录规则。
- 高风险任务的计划、Spec、TDD 和验证要求。
- 需要稳定恢复的任务入口。

注意：Codex 自动加载的是指令文件，不是所有 Markdown。普通文档如果没有被 `AGENTS.md`、用户 prompt 或工具探索指向，不会天然进入上下文。

### 当前对话

当前对话包括用户刚说的话、前面已经确认的目标、Codex 已经给出的计划、运行过的工具结果和当前任务状态。

这解释了为什么同一句话在新会话和旧会话中的效果不同：旧会话里可能已有计划、文件观察和失败命令，新会话则需要重新发现。

### Skills 摘要

Codex 会知道当前可用 skills 的名称、描述和路径。这个阶段通常只是摘要，不会把每个 `SKILL.md` 的完整内容全部塞进上下文。

当用户显式点名某个 skill，或任务匹配 skill 描述时，Codex 才会读取对应 `SKILL.md`，再按 skill 的流程执行。

因此，skill 的价值不是“让所有知识自动进入上下文”，而是用简短描述触发一个可复用工作流，再按需展开详细说明、脚本和参考资料。

### 可选 memories

memories 是可选的本地记忆层，用来保存稳定偏好、常用工作流、技术栈和项目习惯。

它不应替代 `AGENTS.md` 或仓库文档。必须长期、稳定、团队共享的规则，应写进仓库；memories 更适合个人偏好和辅助召回。

## Codex 不会自动看到什么

下面这些内容即使存在于仓库里，也不等于 Codex 已经读取：

- 所有源码。
- 所有 `docs/` 文档。
- 所有 `docs/changes/` 历史记录。
- 所有测试文件。
- 所有 YAML、TOML、JSON 配置。
- 普通规划文档，例如 `docs/specs/xxx.md`、`docs/notes/xxx.md`。

它们会在以下情况下被读取：

- 用户明确提到路径或使用 `/mention`。
- `AGENTS.md` 要求开始某类任务时读取。
- 文件名、目录名或 README 索引显示它们高度相关。
- `rg`、`rg --files`、`find`、`git diff` 等工具探索把它们指向当前问题。
- 某个 skill、MCP 工具或恢复流程要求读取。

所以，不要把“仓库有这份文档”理解成“Agent 一定知道这份文档”。更可靠的做法是把关键入口写进 `AGENTS.md`、README 或任务 prompt。

## 文件发现逻辑

Codex 处理一个任务时，通常不会直接打开大量文件，而是先从低成本的结构探索开始。

常见顺序是：

```text
先看用户目标
-> 查看 git 状态或当前工作区
-> 列文件名或搜索关键词
-> 读取 README、AGENTS、入口文档或相关配置
-> 根据观察结果继续追踪源码、测试、变更记录或命令输出
```

常见命令包括：

```bash
git status --short
rg --files
rg -n "关键词" .
find docs -type f
sed -n '1,200p' README.md
```

这些命令的目的不同：

| 命令 | 作用 |
| --- | --- |
| `git status --short` | 识别当前是否已有未提交改动，避免覆盖用户工作。 |
| `rg --files` | 快速了解仓库文件结构，找入口文件和可能相关文件。 |
| `rg -n "关键词" .` | 定位符号、配置项、文档标题、错误信息或业务词。 |
| `find docs -type f` | 在文档目录中寻找规划、Spec、变更记录和说明。 |
| `sed -n` / `cat` | 读取少量关键文件内容。 |

Codex 是否继续深入，取决于任务类型和观察结果。文件名已经足够明确时，读取会少；问题来源不明、涉及 bugfix 或跨模块影响时，读取会多。

## 怎么描述“扫描多”

当你希望 Codex 不要只看一个文件，而是主动定位上下文，可以明确要求“广泛探索”。

适合场景：

- 不知道问题来自哪个模块。
- 需要定位 bug 根因。
- 需要理解调用链、配置链或历史变更。
- 需要判断某篇文档应该放在哪里。
- 需要继续一个中断很久的任务。

推荐表达：

```text
请先广泛探索仓库结构，使用 `rg --files` 和关键词搜索定位相关入口、配置、测试和历史变更；读取关键文件后再判断根因和给出计划。
```

更强约束：

```text
先不要修改文件。请按 ReAct 小步排查：每轮只做搜索、读文件或运行验证之一；必须基于真实观察说明当前判断。
```

这类 prompt 会引导 Codex：

- 列出文件结构。
- 搜索相关关键词。
- 读取 README、AGENTS、Spec、变更记录或入口源码。
- 追踪调用链和测试。
- 在改动前先形成根因或当前状态判断。

## 怎么描述“扫描少”

当你已经知道目标文件，或者任务只是局部文档整理，应主动收敛范围。

适合场景：

- 修改指定文档。
- 审查一个配置模板。
- 解释单个文件。
- 根据用户给出的片段做小改。
- 避免 Agent 把任务扩大成全仓库重构。

推荐表达：

```text
请只围绕 `context-management.md` 和 `README.md` 处理，不要扫描无关 demo 目录；如发现必须扩大范围，先说明理由再继续。
```

更强约束：

```text
本任务只允许读取和修改我指定的文件。不要运行全仓库搜索，不要改动未提到的文档。
```

这类 prompt 会引导 Codex：

- 优先读取指定文件。
- 减少无关搜索。
- 不追踪不相关目录。
- 只在必要时请求扩大范围。

## 普通规划文档为什么有时会被发现

普通规划文档不会自动进入上下文，但它可能被工具探索发现。

例如：

```text
docs/00-1-learning-roadmap.md
docs/specs/2026-06-08-learning-plan.md
docs/notes/agent-runtime.md
```

当用户说“继续规划下一阶段学习内容”时，Codex 可能会先做：

```bash
rg --files
find docs -type f
rg -n "学习|路线图|roadmap|plan" docs
```

如果文件名和目录足够明确，它就会被选中读取。

但这种发现依赖启发式判断，不如显式规则稳定。更可靠的做法是在 `AGENTS.md` 中写清楚任务恢复入口。

推荐模板：

```md
## 任务恢复入口

继续学习规划或阶段推进时，优先读取：

- `docs/00-1-learning-roadmap.md`
- 最新的 `docs/changes/YYYY-MM-DD-*.md`
- 如存在 `.planning/.active_plan`，再读取对应 `task_plan.md`、`findings.md`、`progress.md`
```

这样 Codex 不需要猜，跨会话恢复会稳定很多。

## 工具调用和观察反馈

Codex 的关键能力不是模型自己能执行命令，而是 runtime 给模型暴露了受控工具。

典型循环是：

```text
模型判断下一步需要观察什么
-> 生成工具调用
-> runtime 按权限执行命令或读取文件
-> 工具结果作为 Observation 回到上下文
-> 模型基于 Observation 继续判断
```

对工程任务来说，常见工具动作包括：

- 搜索文件和关键词。
- 读取源码、文档、配置和测试。
- 运行测试、lint、构建或格式检查。
- 检查 diff 和 git 状态。
- 调用 MCP、CodeGraph、浏览器或外部文档工具。

每次工具调用都会增加上下文事实，也会消耗 token。好的任务描述不是让 Codex 什么都扫，而是说明它应该围绕什么目标探索、探索到什么程度、什么时候停止。

## 任务状态如何沉淀

Codex 的当前会话可以保存短期状态，但长期任务不能只靠聊天记录。

稳定的状态沉淀通常分三层：

| 层级 | 用途 |
| --- | --- |
| 当前会话 | 保存本轮目标、工具结果、计划、验证输出和正在执行的步骤。 |
| `/compact` / `codex resume` | 压缩或恢复历史会话，适合继续同一条任务线。 |
| 仓库文档 | 通过 `docs/changes/`、Spec、计划文件沉淀跨会话、跨 agent 可见事实。 |

文件型计划的价值在于把任务状态固定成约定文件：

```text
task_plan.md
findings.md
progress.md
.planning/.active_plan
```

如果对应 skill、hook 或 `AGENTS.md` 规则要求“开始任务先读这些文件”，新会话就更容易恢复到具体任务，而不是靠文件名猜测。

## 可复制提示词

### 广泛定位

```text
请先不要修改文件。先用 `git status --short`、`rg --files` 和关键词搜索定位相关入口、配置、测试和历史变更；读取关键文件后再说明当前判断、计划和验证方式。
```

### 指定文件修改

```text
只围绕 `README.md` 和 `context-management.md` 修改。不要扫描无关目录；如果必须扩大范围，先说明原因。
```

### 继续历史任务

```text
继续上次学习规划。请优先读取 `AGENTS.md`、学习路线图文档和最新 `docs/changes/`，确认当前阶段后再给计划。
```

### Bugfix 根因定位

```text
请按 ReAct 小步排查这个 bug：先复现或读取失败输出，再搜索相关调用链和测试；定位根因前不要改代码。修改后必须运行对应验证命令。
```

### 文档整理

```text
请先判断这段内容属于哪类文档边界，再决定是补到现有文档还是新建文章。给出根因和取舍后再执行最小修改。
```

### 限制探索范围

```text
本任务只允许读取我指定的文件，不要全仓库搜索，不要修改未提到的文件。发现范围不足时先停下来说明缺失信息。
```

## 一句话版

`AGENTS.md` 是显式协作规则，skills 是可触发的工作流，memories 是可选个人记忆，仓库文件是需要通过工具探索或用户指定才会进入上下文的事实来源。

Codex 不会默认读取完整仓库；它会先加载明确规则，再根据任务目标选择性搜索、读取、执行和验证。
