# AI 编程工作流

本文记录 AI 编程协作的方法论总纲，用于决定不同任务应该采用哪种协作模式，以及如何用验证、上下文沉淀和仓库约束提升 agent 执行质量。

它不是命令手册。具体 Codex 命令见 [Codex 核心指令](./codex-core-commands.md)，skills 安装与触发见 [Codex Skills 使用笔记](./codex-skills.md)。

## 核心结论

AI 编程的稳定性不只取决于提示词，而取决于工作流和 harness engineering：

- 工作流决定人类与 agent 的授权边界。
- Harness engineering 决定 agent 能否在仓库内稳定执行。
- 文档负责导航和解释，验证工具负责阻塞错误。
- 每次改动都应留下可恢复的上下文，方便后续 `resume` 和其他 agent 接手。

## 三种协作模式

### Vibe coding：一句话全权委托

Vibe coding 适合低风险、小范围、可快速回滚的任务。用户只给一句目标，agent 自行定位、修改和验证。

适用场景：

- 文案、小型 README 调整、简单示例补充。
- 明确的 typo、路径修正、轻量配置说明。
- 一次性脚本、草稿、原型或不影响生产路径的内容。

风险：

- Agent 可能扩大范围。
- 用户中途没有刹车点。
- 如果缺少验证，容易把“看起来完成”误当成“真正完成”。

使用要求：

- 必须限制改动范围。
- 必须执行可用验证。
- 必须检查 diff，避免无关重排。

### Plan 模式：中间一次刹车

Plan 模式适合日常开发中的中等复杂任务。Agent 先定位上下文、给出计划和验证方式，用户确认后再执行。

适用场景：

- 单模块 bugfix。
- 普通功能开发。
- 小范围重构。
- 涉及多个文件但边界清楚的改动。

计划应至少包含：

- 根因或当前状态判断。
- 将修改的文件或模块。
- 执行步骤。
- 验证命令。
- 风险边界和不做什么。

Plan 模式的价值是给人类一次刹车机会，避免 agent 在未对齐目标时直接改代码。

### Spec 模式：边界、方案、进度

Spec 模式适合高风险、高不确定性或跨模块任务。它不是为了写长文档，而是为了提前锁定不可逆成本高的决策。

三阶段：

1. 边界：明确做什么、不做什么、成功标准、风险点。
2. 方案：明确架构取舍、数据流、接口变化、文件边界和验证方式。
3. 进度：拆解任务、记录完成状态、失败原因和下一步动作。

适用场景：

- 新功能或新模块。
- 跨模块重构。
- 数据迁移、权限、支付、安全等高风险改动。
- 长任务、多天任务或需要多人/多 agent 接手的任务。

Spec 模式的关键不是文档长度，而是让实现者不需要再猜核心决策。

具体生成流程、文档模板和夜间委托口令见 [Spec 模式实践](./spec-workflow.md)。

## 任务分级模型

推荐按风险选择协作模式：

| 任务类型 | 推荐模式 | 验证要求 |
| --- | --- | --- |
| 文案、小说明、简单示例 | Vibe coding | diff 检查和链接检查 |
| 单模块 bug、小功能、小重构 | Plan 模式 | 测试、lint、typecheck 或等价检查 |
| 跨模块功能、架构调整、迁移 | Spec 模式 | 分阶段验证和回归检查 |
| 生产故障、测试大面积失败 | Systematic debugging | 先定位根因，再修复和验证 |

如果任务风险判断不清，默认升一级处理：从 vibe 升到 plan，从 plan 升到 spec。

## TDD 的位置

TDD 是高价值实践，但本仓库不把它设为所有任务的硬性要求。

TDD 的完整操作流程、测试清单模板和可复制 prompt 见 [TDD 实践手册](./tdd-workflow.md)。

优先考虑 TDD 的场景：

- 新业务行为。
- Bugfix，尤其是需要防止回归的问题。
- 重构但要求行为不变。
- 边界条件复杂或历史上容易出错的逻辑。

可以用验证替代 TDD 的场景：

- 纯文档变更。
- 配置说明或示例整理。
- 没有自动化测试框架的笔记仓库。
- 明确的一次性原型。

推荐做法：

- 有测试框架时，优先先写失败测试，再实现，再确认测试通过。
- 没有测试框架时，至少记录手动验证命令或检查项。
- 不为了形式化 TDD 引入过重测试结构，但不能跳过验证。

## Harness Engineering 原则

### 地图而非手册

`AGENTS.md` 应该是导航地图，而不是百科全书。建议控制在 300 行以内，重点写：

- 项目结构。
- 常用命令。
- 关键约束。
- 验证方式。
- 需要阅读的文档入口。

当一切都写成“重要”时，agent 很难判断真正优先级。详细领域知识应放在更合适的仓库文档里。

### 嵌入而非外挂

Agent 看不到的知识，对它来说基本不存在。领域知识、架构约束和历史上下文应尽量放进仓库：

- 架构约束：`docs/architecture.md` 或 ADR。
- 接口约束：OpenAPI、proto、schema、类型定义。
- 领域规则：测试、fixtures、示例数据。
- 历史上下文：`docs/changes/YYYY-MM-DD-<topic>.md`。

外部知识库可以作为人类参考，但不应成为 agent 执行任务的唯一依据。

### 机械验证而非人工检查

文档规则是软约束，工具验证是硬约束。能自动检查的规则，应尽量进入验证命令：

- 格式：formatter 或 markdownlint。
- 代码规范：lint。
- 类型：typecheck。
- 行为：unit test、integration test、contract test。
- 配置：schema 校验、TOML/YAML/JSON 解析检查。

Agent 最终应报告执行过什么命令、结果是什么、还有什么没验证。

### 迭代自愈而非等待接入

Agent 改完后不应立刻把失败交给人类。推荐闭环：

```text
修改 -> 验证 -> 失败 -> 分析根因 -> 修复 -> 再验证
```

建议自动收敛 1 到 3 轮。超过 3 轮仍失败时，应停止并交还人类，说明：

- 失败命令。
- 关键错误。
- 已尝试的修复。
- 已排除的原因。
- 需要人类决策的问题。

## AI 权限边界

AI 协作需要默认权限，也需要明确停止线。权限边界越清楚，越不容易出现误删、越界重构或把本地状态搞乱的问题。

默认允许 AI 执行：

- 读取仓库文件、检索代码和查看文档。
- 运行非破坏性检查命令，例如测试、lint、typecheck、构建和文档链接检查。
- 基于当前上下文提出计划、风险、验证方式和替代方案。
- 在用户授权后，修改任务范围内的文件。

需要用户确认后再执行：

- 删除文件、大规模移动文件或重排目录结构。
- 执行 `git reset`、强制覆盖、清理工作区等破坏性 Git 操作。
- 安装依赖、联网下载、升级主要框架或修改锁文件。
- 数据库迁移、生产配置、发布脚本、CI/CD 权限相关改动。
- 提交 commit、创建 tag、push 远端或发起发布。

始终禁止：

- 写入真实 API Key、Token、密码、私有代理地址或内部服务 URL。
- 伪造测试结果、构建结果或外部资料来源。
- 在未说明风险和边界的情况下扩大任务范围。

## 上下文生命周期

AI 编程不是单轮对话，必须考虑跨天、压缩和交接。

常用机制：

- `/compact`：长会话中压缩关键上下文，适合暂停前或上下文接近上限时使用。
- `codex resume --last`：第二天或重新打开终端后恢复最近会话。
- `docs/changes/YYYY-MM-DD-<topic>.md`：沉淀长期上下文，帮助后续 agent 理解历史改动。

分工原则：

- `/compact` 解决当前会话上下文太长的问题。
- `resume` 解决回到上次会话的问题。
- `docs/changes/` 解决跨会话、跨 agent、跨时间的长期记忆问题。

只要本次会话产生仓库文件改动，就应自动生成或更新日期变更文档。

## 项目落地清单

一个适合 AI 协作的项目，至少应具备：

- `AGENTS.md`：仓库导航地图和硬规则。
- `README.md`：人类入口和内容索引。
- 可执行验证命令：测试、lint、typecheck 或项目等价检查。
- `docs/changes/`：日期变更记录。
- 必要领域文档：架构、接口、部署、数据模型或业务规则。

新项目接入 AI 协作时，可按 [AI 项目落地清单](./ai-project-checklist.md) 逐项检查。

如果只能先做一件事，优先把验证命令和 `AGENTS.md` 写清楚。没有机械验证的项目，agent 很难稳定交付。

## 推荐默认流程

日常任务建议按以下顺序执行：

```text
确认任务风险 -> 选择 vibe/plan/spec -> 定位上下文 -> 执行或计划 -> 验证 -> 自愈 -> 记录变更 -> diff/review
```

跨天任务：

```text
/compact -> 暂停 -> codex resume --last -> /status -> 继续执行
```

Spec 后有两条常用执行路径：

| 路径 | 适用场景 | 推荐流程 |
| --- | --- | --- |
| Spec + Plan | 大多数高风险任务、架构调整、跨模块改动、文档或配置类复杂任务 | `spec 边界 -> spec 方案 -> plan 任务 -> 分阶段执行 -> 机械验证 -> 日期变更记录` |
| Spec + TDD | 核心业务规则、bugfix、重构、权限、安全、数据一致性、复杂边界条件 | `spec 边界 -> spec 方案 -> TDD 测试清单 -> 分阶段执行 -> 机械验证 -> 日期变更记录` |

默认优先使用 Spec + Plan。只有当任务的正确性适合用测试约束时，再升级为 Spec + TDD。
