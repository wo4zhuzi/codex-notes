# Agent 评估与验证

本文记录 AI Agent 任务完成前的验收方法。它不评价回答是否流畅，而是判断任务产出是否有证据、可复现、可交付。

Agent Evaluation 是工作流中的交付验收闸门：

```text
执行任务 -> Agent Evaluation 验收 -> 记录变更 -> diff/review
```

它不替代测试、lint、typecheck、`/diff` 或 `/review`。它的作用是把这些证据组织起来，判断当前任务是否可以继续、收尾或必须交还人类。

## 核心原则

没有证据的完成，不算完成；不能复现的验证，不算验证；没有说明风险的交付，不算可信交付。

评估时应优先回答：

- 目标是否完成。
- 根因或现状判断是否有依据。
- 改动是否限制在任务范围内。
- 验证命令是否执行，结果是什么。
- 哪些内容没有验证。
- 是否生成或更新 `docs/changes/YYYY-MM-DD-<topic>.md`。
- 当前状态是可交付、部分完成，还是必须停止交给人。

## 两类评估

Agent Evaluation 分为阶段性评估和交付评估。两者不能混用。

### 阶段性评估

阶段性评估用于开发过程中，判断下一步能不能继续。

适合场景：

- 刚定位完根因。
- 刚完成一个小步骤。
- 验证失败后准备自愈。
- 长任务准备进入下一阶段。

通过标准：

- 当前上下文足以支持下一步。
- 根因或现状判断有初步证据。
- 当前修改没有明显偏离目标。
- 局部验证结果支持继续推进。
- 未完成内容已明确。

阶段性评估可以接受局部通过，但必须说明它不是最终交付。

示例：

```text
阶段性评估：
- 已确认失败来自订单状态转换逻辑。
- 已补充一个复现测试，目前测试失败符合预期。
- 下一步实现最小修复。
- 尚未运行全量测试，当前不能视为可交付。
```

### 交付评估

交付评估用于最终总结前，判断当前结果能不能收尾。

适合场景：

- 代码或文档改动完成。
- 自愈循环结束。
- 准备生成变更记录、查看 diff 或进入 review。
- 准备汇报任务完成状态。

通过标准：

- 任务目标已完成。
- 根因或现状判断有可追溯依据。
- 修改文件都在任务范围内。
- 对应验证命令已执行并报告结果。
- 未验证项和剩余风险已说明。
- 需要的 `docs/changes/` 变更记录已生成或更新。

交付评估不能只说“应该可以”。如果没有证据，只能标记为部分完成或未完成。

## 评估强度

评估强度由三个维度决定：

```text
阶段 × 项目风险 × 改动类型
```

### 按阶段

| 阶段 | 目标 | 通过标准 |
| --- | --- | --- |
| 开发中间态 | 判断能否继续 | 局部证据成立，下一步明确 |
| 最终验收 | 判断能否收尾 | 完成标准满足，风险和未验证项明确 |

### 按项目风险

| 项目类型 | 评估强度 | 典型验证 |
| --- | --- | --- |
| 个人笔记或文档项目 | 轻量 | 状态检查、链接检查、敏感信息扫描、变更记录 |
| 普通应用项目 | 中等 | 相关测试、lint、typecheck、build、关键流程验收 |
| 生产核心项目 | 严格 | 单测、集成测试、配置 dry-run、权限检查、回滚方案、人工确认 |

项目越接近生产，越涉及权限、数据、支付、发布和安全，评估越重。

### 按改动类型

不同改动类型选择不同验收清单。不要把文档任务做成生产发布检查，也不要把权限改动按普通文档任务处理。

## 按任务类型验收

### 文档任务

阶段性评估重点：

- 内容方向是否符合目标。
- 新增结构是否和现有文档一致。
- 是否发现明显重复或冲突。

交付评估建议检查：

```bash
git status --short
rg -n "TODO|FIXME|your-api-key|sk-" .
test -f <新增文档>
rg -n "<新增文档名|关键标题>" README.md *.md
```

验收问题：

- 新增链接是否指向真实文件。
- 是否写入真实 API Key、Token、私有地址或内部服务 URL。
- 是否生成 `docs/changes/` 变更记录。
- 是否说明未运行的可选检查，例如 markdownlint。

### Bugfix

阶段性评估重点：

- 是否复现问题。
- 根因是否来自真实代码、日志或测试结果。
- 修复方向是否能解释原始失败。

交付评估建议检查：

- 原失败用例是否通过。
- 新增或已有回归测试是否覆盖关键路径。
- 相关测试集是否通过。
- 修改是否限制在根因相关范围内。
- 是否说明未覆盖的边界条件。

推荐流程：

```text
复现失败 -> 定位根因 -> 补测试或确认已有测试 -> 最小修复 -> 验证失败用例 -> 验证相关测试集
```

### 重构

阶段性评估重点：

- 外部行为是否保持不变。
- 调用方和影响范围是否查清。
- 是否避免无关格式化和重排。

交付评估建议检查：

- 相关测试是否通过。
- public API、配置键、数据结构是否未意外变化。
- diff 是否主要体现结构调整。
- 是否记录无法自动验证的行为风险。

重构任务不能只用“代码更清晰”作为完成标准。必须证明行为没有被意外改变。

### 配置任务

阶段性评估重点：

- 配置项含义是否确认。
- 默认值变化是否明确。
- 是否涉及生产、权限或发布流程。

交付评估建议检查：

- TOML、YAML、JSON 或对应配置格式是否合法。
- 配置能否被加载、dry-run 或示例命令验证。
- 是否包含真实 token、私有地址或内部服务 URL。
- 是否说明默认值变化和兼容影响。

涉及生产配置时，必须有人类确认，不能只由 agent 自动收尾。

### RAG / MCP / Function Calling

阶段性评估重点：

- Agent 是否真的需要调用外部能力。
- tool/resource/function 的输入输出是否清楚。
- 权限边界和失败路径是否明确。

交付评估建议检查：

- 正常路径是否可用。
- 参数错误是否有结构化处理。
- 工具失败或超时是否有降级说明。
- 权限不足是否不会泄露敏感信息。
- 回答是否基于工具结果，而不是模型猜测。

这类任务的评估重点不是“模型答得像不像”，而是工具调用链是否可控、可解释、可失败。

### Code Review

阶段性评估重点：

- 是否基于真实 diff。
- 是否理解需求背景。
- 是否优先寻找行为风险，而不是泛泛评价风格。

交付评估建议检查：

- findings 是否有文件和行号。
- 是否按严重程度排序。
- 是否区分缺陷、风险、测试缺口和开放问题。
- 没有发现问题时，是否说明残余风险或未覆盖范围。

## 可复制验收 Prompt

阶段性评估：

```text
先不要继续修改。请按 Agent Evaluation 做阶段性评估：
1. 当前步骤的目标是什么？
2. 已确认的事实和依据是什么？
3. 当前结论是否足以支持下一步？
4. 已运行哪些局部验证，结果是什么？
5. 哪些内容还没完成或没验证？
6. 下一步应该继续、调整计划，还是停止交给人？
```

交付评估：

```text
先不要总结。请按 Agent Evaluation 检查本次任务：
1. 当前是阶段性评估还是交付评估？
2. 本任务属于什么项目风险和改动类型？
3. 目标是否完成？
4. 根因或现状判断依据是什么？
5. 修改了哪些文件，是否都在任务范围内？
6. 运行了哪些验证命令，结果是什么？
7. 哪些内容没有验证？
8. 是否需要生成或更新 docs/changes/？
9. 当前是可交付、部分完成，还是必须停止交给人？
```

## 交付状态

### 可交付

满足以下条件时，才能说可交付：

- 目标完成。
- 验证命令已执行并通过，或未通过项已解释且不阻塞交付。
- 改动范围符合任务边界。
- 变更记录已生成或无需生成的理由明确。
- 剩余风险可接受。

### 部分完成

出现以下情况时，只能说部分完成：

- 完成了部分步骤，但关键验证未运行。
- 缺少测试环境、依赖或权限。
- 有未解决风险，但不阻塞继续开发。
- 需要用户确认产品、架构或业务取舍。

### 必须停止并交给人

出现以下情况时，应停止继续自动推进：

- 连续 3 轮验证失败仍无法收敛。
- 需要生产权限、发布权限或敏感配置。
- 涉及数据迁移、支付、权限或安全边界，但需求不明确。
- 工具输出和代码事实冲突，无法判断可信来源。
- 继续修改可能覆盖用户已有未提交改动。

## 与现有工作流的关系

Agent Evaluation 补的是“交付判断”：

- [AI 编程工作流](./ai-workflow.md)：决定用什么协作模式。
- [Agent 上下文管理](./context-management.md)：决定给 agent 什么输入。
- [Spec 模式实践](./spec-workflow.md)：明确高风险任务的目标、范围和方案。
- [TDD 实践手册](./tdd-workflow.md)：用测试锁定核心行为。
- Agent Evaluation：判断 agent 做完后，凭什么说可以交付。

推荐闭环：

```text
选择协作模式 -> 定位上下文 -> 明确计划和验收标准 -> ReAct 小步执行 -> 阶段性评估 -> Harness 验证 -> 交付评估 -> 记录变更 -> diff/review
```
