# Agent 上下文管理

本文记录 AI Agent 协作中的上下文管理方法。它不是替代 Harness Engineering，而是其中的基础模块：上下文管理决定 agent 看见什么，Harness Engineering 决定 agent 如何被约束、验证和恢复。

## 核心目标

上下文管理的目标不是给 agent 塞入尽可能多的信息，而是让它在当前任务中看到足够、准确、可验证的信息。

好的上下文应回答四个问题：

- 当前任务要解决什么问题。
- 相关事实来自哪些文件、日志、命令或历史记录。
- 哪些约束不能被突破。
- 修改后如何验证结果。

如果上下文缺失，agent 容易猜测；如果上下文过多，agent 容易误读无关信息；如果上下文过期，agent 可能沿着错误前提继续执行。

## 上下文选择

不同任务需要不同粒度的上下文。建议优先从任务本身出发，而不是默认全仓库扫描。

### 让 agent 自己定位

适合范围不明确的问题，例如：

- 不知道报错来自哪个模块。
- 需要理解某个功能的完整调用链。
- 怀疑多个文件共同影响行为。

推荐方式：

```text
先搜索相关入口、配置和测试，定位根因后再给出计划。
```

这种方式适合 bugfix、复杂重构和陌生仓库探索，但需要要求 agent 先观察再下结论。

### 使用 `/mention` 收敛范围

适合已经知道相关文件或目录的任务，例如：

- 解释某个文件。
- 修改指定文档。
- 审查一个配置模板。
- 分析某个测试失败。

推荐方式：

```text
/mention README.md
帮我检查这里的学习路线是否还有缺口。
```

`/mention` 的价值是减少无关搜索，让 agent 围绕指定上下文工作。范围明确时优先使用它；范围不明确时先让 agent 搜索定位。

### 主动提供关键材料

以下材料通常比口头描述更可靠：

- 真实错误日志和执行命令。
- `git diff` 或相关改动文件。
- API 契约、schema、proto、OpenAPI 或类型定义。
- 测试失败输出。
- `docs/changes/` 中的历史变更记录。
- 业务规则、边界条件和验收标准。

不要只说“这个功能坏了”。更好的表达是：

```text
运行 `go test ./...` 时 `TestCreateOrder` 失败，错误是重复扣库存。
请先定位库存扣减流程和相关测试，再判断根因。
```

## 上下文结构

面向 agent 的上下文应尽量结构化。推荐按以下顺序组织：

```text
目标：这次要完成什么。
现状：当前行为、错误或已有实现。
范围：哪些文件、模块或流程相关。
约束：不能改什么，必须遵守什么。
已尝试：之前执行过的命令、方案和失败结果。
验证：完成后必须运行什么检查。
```

对于复杂任务，可以要求 agent 在计划中显式包含：

- 根因定位。
- 修改步骤。
- 验证方式。
- 风险边界。
- 是否需要生成 `docs/changes/YYYY-MM-DD-<topic>.md`。

## 上下文生命周期

AI 编程通常不是单轮对话。上下文需要在当前会话、压缩摘要、长期文档之间流动。

### 当前会话

当前会话适合保存短期上下文，例如：

- 本轮任务目标。
- 刚读取的文件内容。
- 最新命令输出。
- 当前计划和执行状态。

当前会话信息最灵活，但也最容易随着上下文变长而失真。长任务中应定期整理关键结论。

### `/compact`

`/compact` 用于压缩当前会话中的关键上下文，适合：

- 会话很长。
- 上下文接近上限。
- 准备暂停后继续同一任务。
- agent 开始重复询问已确认信息。

压缩前应确保摘要包含：

- 当前目标。
- 已确认的根因或现状。
- 已修改和待修改内容。
- 已运行验证和结果。
- 未解决风险。

### `codex resume`

`codex resume` 用于恢复历史会话。常见用法：

```bash
codex resume --last
```

它适合跨天继续任务，但不能替代仓库内文档。会话恢复依赖本地记录，其他 agent 或其他环境不一定能看到。

### `docs/changes/`

`docs/changes/YYYY-MM-DD-<topic>.md` 是长期上下文，适合记录：

- 任务背景。
- 根因定位。
- 执行计划。
- 实际改动。
- 验证结果。
- 后续建议。

只要本次会话产生仓库文件改动，就应创建或更新对应日期变更文档。它是跨会话、跨 agent、跨时间恢复上下文的主要来源。

### 文件型计划

长任务可使用 `planning-with-files` 或类似流程：

- `task_plan.md`：执行前计划。
- `findings.md`：过程上下文、调研发现和取舍。
- `progress.md`：当前任务状态。

这些文件适合长任务续接，但不能替代 `docs/changes/`。任务完成后，仍应把最终事实沉淀到日期变更文档。

## 上下文污染

上下文污染指 agent 接收到不准确、过期或无关的信息，并把它当作当前事实继续推理。

常见来源：

- 过期文档和旧方案。
- 用户临时想法被误认为最终需求。
- 无关 `git diff` 被误认为本次任务内容。
- 错误日志缺少命令、环境或版本信息。
- 上一次任务的结论混入新任务。
- 模型根据常见项目结构猜测当前仓库结构。

降低污染的做法：

- 修改前先运行 `git status --short`，识别已有未提交改动。
- 要求 agent 先搜索或读取真实文件，再给结论。
- 明确说明哪些内容是背景，哪些是最终需求。
- 对历史文档标注日期或更新时间。
- 用验证命令确认结论，而不是只相信上下文推理。

## 常见场景

### Bugfix

推荐上下文：

- 失败命令。
- 完整错误输出。
- 最近相关改动。
- 相关测试和业务规则。

推荐流程：

```text
复现失败 -> 定位相关代码 -> 判断根因 -> 写或补测试 -> 最小修改 -> 验证通过 -> 记录变更
```

### 重构

推荐上下文：

- 重构目标。
- 不应改变的外部行为。
- 调用方和测试覆盖。
- 风险边界。

重构时最重要的是让 agent 区分“结构变化”和“行为变化”。没有测试保护时，应先明确手动验证方式。

### 长任务

推荐上下文：

- 明确计划。
- 阶段性进度。
- 已排除方案。
- 每轮验证结果。

长任务不要只依赖聊天记录。应及时使用 `/compact`、`docs/changes/` 或文件型计划沉淀关键信息。

### RAG 和 MCP

RAG 和 MCP 都是在扩展 agent 的可见世界：

- RAG 让 agent 能检索私有知识。
- MCP 让 agent 能访问工具、资源和外部系统。

这类能力不是越多越好。每个新增数据源或工具都应明确：

- 适用场景。
- 权限边界。
- 输出可信度。
- 失败时的降级方式。
- 是否需要人工确认。

### 代码审查

推荐上下文：

- 真实 diff。
- 需求或 issue 背景。
- 测试结果。
- 影响范围。

审查时应优先关注行为风险、遗漏测试、兼容性和安全边界，而不是泛泛评价代码风格。

## 与 Harness Engineering 的分工

上下文管理和 Harness Engineering 不应拆成两套流程：

- 上下文管理提供正确输入。
- Harness Engineering 提供执行约束。
- 测试、lint、typecheck、diff 和 review 提供机械验证。
- `docs/changes/` 提供长期记忆。

推荐默认流程：

```text
定位上下文 -> 判断根因或现状 -> 制定计划 -> 执行修改 -> 运行验证 -> 自愈 -> 记录变更 -> review
```

在真实工程中，agent 的可靠性通常不是来自更长的提示词，而是来自准确上下文、清晰边界和可执行验证的组合。
