# Codex 核心指令

本文记录 Codex 日常协作中最常用的核心指令，并说明推荐使用顺序。目标是让每次任务都先建立上下文，再规划、执行、记录、核对和审查，减少遗漏和误改，并让后续 AI 能理解历史改动背景。

## 推荐工作流

```text
codex resume --last 或 /init -> /status -> /plan -> AI 检查计划 -> 执行任务 -> /compact -> 生成日期变更文档 -> /diff -> /review
```

- 每次执行前都应先规划；即使是小改动，也至少用简短计划明确目标、范围和验证方式。
- 计划生成后必须让 AI 自检一次，确认计划包含根因定位、修改步骤、验证方式和风险边界。
- 如果是延续昨天或上次未完成任务，优先使用 `codex resume --last` 恢复最近会话，再用 `/status` 确认上下文。
- 如果本次会话产生任何仓库文件改动，执行完成后必须自动在 `docs/changes/` 下创建或更新以日期开头的变更文档，沉淀本次会话的上下文。
- 长会话、上下文接近上限或准备跨天继续时，应使用 `/compact` 压缩上下文，避免后续恢复时丢失关键决策。
- 提交前使用 `/diff` 核对真实改动，再使用 `/review` 做最后一轮风险检查。完整 Git 分支、worktree、commit 和 push 规则见 [Git 融入 AI 工作流](./git-workflow.md)。

## resume：恢复上次会话

`resume` 用于恢复之前的 Codex 会话，适合昨天任务没做完、关机后第二天继续，或中途关闭终端后回到原任务。

推荐命令：

```bash
codex resume --last
```

典型场景：

- 昨天任务未完成，第二天继续同一个项目。
- 终端或电脑关闭后，需要恢复最近一次 Codex 上下文。
- 长任务拆成多天执行，希望保留之前的计划、讨论和执行状态。

恢复后建议立即执行：

```text
/status
```

检查重点：

- 当前目录是否仍是目标项目。
- AI 是否正确理解上次任务目标、已完成内容和剩余步骤。
- 工作区是否存在未提交或未记录的文件变更。
- 是否需要先阅读 `docs/changes/` 下最近的日期变更文档。

如果不是恢复最近一次会话，而是要选择更早的会话，应先查看 Codex 客户端提供的会话列表或历史入口，再选择对应会话恢复。

### 本机 session 存储位置

Codex 会话文件通常存放在用户主目录下的 `.codex` 目录。本机当前观察到的主要位置如下：

```text
~/.codex/sessions/YYYY/MM/DD/rollout-<timestamp>-<session-id>.jsonl
~/.codex/session_index.jsonl
~/.codex/history.jsonl
~/.codex/state_5.sqlite
~/.codex/logs_2.sqlite
```

示例路径：

```text
~/.codex/sessions/2026/05/17/rollout-2026-05-17T09-26-21-019e338a-9986-76f1-a96b-c4a98ba13bda.jsonl
```

说明：

- `~/.codex/sessions/`：按年月日保存会话 JSONL，是 `codex resume` 恢复上下文的重要来源。
- `~/.codex/session_index.jsonl`：会话索引文件，可能用于快速定位历史会话。
- `~/.codex/history.jsonl`：本地交互历史记录。
- `~/.codex/state_5.sqlite`、`~/.codex/logs_2.sqlite`：本地状态和日志数据库。

这些文件可能包含提示词、命令、路径、代码片段和对话内容，不建议手动编辑，也不要提交到项目仓库。排查恢复问题时优先使用 `codex resume --last` 或客户端历史入口；只有在需要确认本机是否保存过会话时，才查看这些路径。

## /compact：压缩会话上下文

`/compact` 用于把长会话中的关键上下文压缩成摘要，降低上下文过长导致遗漏、跑偏或恢复困难的风险。

建议在以下场景使用：

- 长任务执行中，已经经历多轮定位、方案调整或文件修改。
- 准备暂停任务，预计之后通过 `codex resume --last` 继续。
- 上下文接近上限，或 AI 开始重复询问已经确认过的信息。
- 已完成阶段性工作，需要保留关键决策、剩余任务和验证状态。

压缩前应确保关键信息已经明确：

- 当前目标和不做什么。
- 已定位的根因或关键判断。
- 已修改文件和剩余待改文件。
- 已执行的验证命令和结果。
- 下一步最小可执行动作。

`/compact` 不是替代 `docs/changes/` 的长期记录。只要本次会话产生仓库文件改动，仍必须在结束前生成或更新日期变更文档。

## Shift + Tab：切换协作模式

在支持模式切换的 Codex 终端界面中，使用 `Shift + Tab` 切换当前会话的协作模式，例如从 Plan Mode 回到默认执行模式，或进入更偏规划的协作模式。

常见用法：

```text
Shift + Tab
```

按下快捷键后根据界面提示选择目标模式。需要从 Plan Mode 回到之前的默认执行模式时，选择 `Default` 或普通协作模式。

使用规则：

- `Shift + Tab` 只影响当前会话的协作模式，不等同于修改仓库文件。
- 如果客户端或运行环境不支持该快捷键，需要重新开启普通会话，或通过客户端提供的模式切换入口处理。
- 进入 `/plan` / Plan Mode 后，模式内的模型和推理强度可能独立于全局配置；如果全局已配置 `high`，但进入 Plan Mode 后显示为 `medium`，应先进入 Plan Mode，再执行 `/model` 重新选择目标模型和推理强度，并选择永久覆盖该模式下的设置。
- 从 Plan Mode 切回默认模式后，才能继续执行会修改仓库文件的任务；在 Plan Mode 中应只做方案规划和非破坏性探索。
- 模式切换后仍应遵循本仓库工作流：执行前计划，执行后生成 `docs/changes/YYYY-MM-DD-<topic>.md`。

## 会话变更文档

每次会话产生仓库改动后，都应新增一份变更文档，路径建议为：

```text
docs/changes/YYYY-MM-DD-<topic>.md
```

命名规则：

- `YYYY-MM-DD` 使用当天日期，例如 `2026-05-16`。
- `<topic>` 使用小写英文短横线，描述本次会话主题，例如 `codex-workflow`。
- 同一天同主题如需多次记录，可追加序号，例如 `2026-05-16-codex-workflow-2.md`。

文档建议包含：

- 任务背景：用户目标和本次改动要解决的问题。
- 根因定位：为什么需要这次修改，当前文件或流程存在什么缺口。
- 执行计划：本次实际采用的计划和 AI 自检结论。
- 变更内容：列出改动文件和关键变化。
- 验证结果：记录执行过的检查命令、结果和未验证项。
- 后续建议：只记录与本次改动直接相关的可选后续事项。

这些文档用于帮助后续 AI 和开发者快速恢复上下文，不应写入密钥、Token、私有地址或无关聊天内容。

自动生成规则：

- 只要本次会话实际修改、新增或删除了仓库文件，AI 必须在结束前自动生成或更新对应的 `docs/changes/YYYY-MM-DD-<topic>.md`。
- 不需要用户再次提醒“生成日期变更文档”。
- 最终回复可以简短说明文档路径，但不能用口头总结替代仓库内文档。

## 新项目全局启用方式

如果希望新开项目也默认执行同一套规则，应把以下内容写入 Codex 全局指令或 `/init` 使用的 `AGENTS.md` 模板中：

```text
执行会产生仓库文件改动的任务时，必须先规划并自检计划。计划应包含根因定位、修改步骤、验证方式和风险边界。

如果本次会话产生任何仓库文件改动，必须在结束前自动创建或更新 docs/changes/YYYY-MM-DD-<topic>.md。不要等待用户额外要求，也不要只在最终回复中用文字描述替代文档。

变更文档应包含：任务背景、根因定位、执行计划、变更内容、验证结果和后续建议。不得写入真实 API Key、Token、私有代理地址或内部服务 URL。
```

落地方式：

- 项目级：在新项目运行 `/init` 后，把上述规则写入该项目的 `AGENTS.md`，适合只约束单个仓库。
- 全局级：把上述规则写入本机 Codex 的全局指令或常用 `AGENTS.md` 初始化模板，适合所有新项目默认继承。
- 强校验：需要团队强制执行时，可额外增加提交前检查，验证本次改动是否包含 `docs/changes/` 下的日期文档。

## /init：初始化项目上下文

`/init` 用于让 Codex 读取当前仓库结构，并生成或完善仓库级协作指引文件 `AGENTS.md`。

`AGENTS.md` 不应只保留模板内容，应结合当前项目补充以下信息：

- 项目结构：源码、测试、配置、文档和资源文件所在目录。
- 构建与运行命令：如 `npm run dev`、`make test`、`go test ./...`。
- 编码风格：缩进、命名规则、格式化工具、lint 工具。
- 测试规范：测试框架、测试文件命名、覆盖率或关键场景要求。
- 提交规范：提交信息格式、PR 描述、关联 issue、截图或验证结果。
- 安全要求：密钥、Token、私有地址和本地配置文件的处理规则。

补充规则：优先写仓库真实存在的命令和目录；没有自动化测试或构建流程时要明确说明；不要编造不存在的工具链。

## /status：确认当前状态

`/status` 用于确认当前会话、任务和工作区状态。执行任务前使用它，可以帮助判断 Codex 是否理解当前目标，以及是否存在未处理的上下文或文件变更。

建议在以下场景使用：

- 开始一个新任务前。
- 长任务中途切换目标前。
- 发现回答或执行方向可能偏离时。
- 准备提交或结束任务前。

## /permissions：管理读写和命令权限

`/permissions` 用于查看或调整 Codex 当前的文件读写、命令执行和审批策略。它决定 Codex 能不能直接改文件、能不能运行命令，以及哪些操作需要先问用户。

建议在以下场景使用：

- 第一次进入新项目，不确定当前会话权限边界。
- 生产项目、老项目、没有 Git 保护的项目，需要先保持保守授权。
- 任务涉及批量修改、删除、迁移、依赖安装或可能影响本机环境的命令。
- 发现 Codex 无法写文件、无法运行命令，或频繁请求审批时。

新手建议先让 Codex 读项目、解释方案和列出风险，不要一开始就放开全自动执行。确认当前状态时，可以先用 `/status` 查看会话信息，再用 `/permissions` 调整权限。

## /plan：制定执行计划

`/plan` 是每次任务执行前的默认步骤。它的作用不是直接改代码，而是先拆解执行路径、风险点和验证方式。任务越小，计划越短；任务越复杂，计划越细。

推荐用于：

- 所有会产生文件修改的任务。
- 新功能开发。
- 跨模块重构。
- 问题排查链路较长的 bug。
- 涉及配置、部署或兼容性的改动。

计划通常以对话中的任务清单或步骤列表存在，例如：

```text
1. 阅读相关模块，定位入口和依赖关系。
2. 修改核心逻辑，保持现有接口不变。
3. 补充或更新测试用例。
4. 运行测试并检查 diff。
```

在支持任务面板的环境中，Codex 也可能把计划同步成可更新的任务状态，例如 `pending`、`in_progress`、`completed`。这类计划主要用于当前会话的执行跟踪；任务完成后，应把最终采用的计划摘要写入 `docs/changes/YYYY-MM-DD-<topic>.md`。

使用规则：

- 先让 Codex 输出计划，不要立刻进入修改，除非用户明确要求直接执行且任务无需修改文件。
- 检查计划是否包含定位、修改、验证和风险说明。
- 如果计划缺少测试或验收步骤，应要求补充。
- 确认方案后，需要明确让 Codex 从规划切回执行，例如“按这个计划执行”。
- 执行过程中应按计划逐项推进；如果发现根因或范围变化，应先更新计划，再继续执行。

不要把 `/plan` 当成提交前检查工具。它更适合在动手前统一目标、边界和执行顺序。

## /mention：指定文件或上下文

`/mention` 用于把某个文件、目录或上下文明确交给 Codex，减少它在全项目中无谓搜索或误读无关文件。

典型用法：

```text
/mention src/api/user.ts
帮我解释这个文件的请求流程。
```

适合以下场景：

- 已经知道报错文件或相关组件。
- 只希望 Codex 分析某个配置文件、模块或测试文件。
- 小问题不需要全项目扫描，希望先围绕指定文件判断。
- 担心 Codex 读取过多无关上下文，导致结论跑偏。

如果问题范围还不明确，可以先让 Codex 搜索定位；如果已经有明确文件，优先用 `/mention` 约束上下文。

## AI 检查计划

计划生成后，应让 AI 基于以下检查项做一次自检，确认计划可以执行：

- 根因是否已定位，是否避免在未读代码或文档前直接下结论。
- 执行步骤是否能直接落地，是否明确涉及的文件或目录。
- 是否包含验证方式，例如测试命令、文档链接检查或配置格式检查。
- 是否说明风险边界，例如不修改无关文件、不重排配置、不提交敏感信息。
- 是否需要产出 `docs/changes/YYYY-MM-DD-<topic>.md` 变更文档。

自检发现缺口时，应先修正计划，再进入执行。

## /side：临时侧聊

`/side` 用于在不打断当前主线任务的情况下临时问一个问题。它适合短问题和快速判断，不适合长期保存另一条实现路线。

典型用法：

```text
/side 这个方案有没有明显风险？
```

适合以下场景：

- 临时确认一个方案风险。
- 问一句概念解释或 API 含义。
- 让 Codex 从 reviewer 视角快速看一眼想法。
- 不希望把短问题混进当前主线任务。

不建议把 `/side` 用于复杂多轮讨论、替代方案实现或长期实验。如果需要认真尝试另一套路线，应使用 `/fork`。

## /fork：复制当前会话开新路线

`/fork` 用于基于当前上下文复制出一条新的会话路线，适合在不破坏主线讨论的前提下尝试另一种方案。

推荐用于：

- 比较两种实现方案。
- 尝试另一种架构或重构路径。
- 保留当前主线，同时让 Codex 用不同思路分析问题。
- 长任务中需要开实验线，但之后仍可能回到原会话。

一句话区分：`/side` 是临时小窗口，`/fork` 是真正开一条新路线。需要回到原来的会话时，使用 `resume` 相关入口恢复原会话。

`/fork` 会复制 fork 当时的上下文，新旧会话之后各自独立发展。例如当前上下文是 `1、2、3`，fork 后在新会话继续加入 `4`：

```text
老会话：1、2、3
fork 新会话：1、2、3、4
```

下次执行 `codex resume --last` 时，通常会恢复最近活跃或最近保存的会话。如果 fork 后一直在新会话里工作，`--last` 大概率恢复的是 `1、2、3、4`，不是 fork 前的 `1、2、3`。

找回 fork 前的老会话时，建议使用：

```bash
codex resume
```

这个命令默认按当前项目目录过滤，并显示可选择的历史会话列表。不要为了只看当前项目会话使用 `codex resume --all`，因为 `--all` 会取消当前目录过滤，显示所有项目的会话。

命令边界：

- `codex resume`：显示当前项目相关会话选择器，推荐用于找 fork 前老会话。
- `codex resume --last`：直接恢复最近会话，适合明确要继续最新路线时使用。
- `codex resume --all`：显示所有项目会话，不适合只筛当前项目。
- `codex resume <SESSION_ID>`：已知会话 ID 时，直接恢复指定会话。

## /new 和 /clear：切换或清理会话

`/new` 和 `/clear` 都适合在当前任务已经结束、准备换新话题时使用。它们和 `/compact` 的边界不同：`/compact` 是压缩历史继续当前任务，`/new` 或 `/clear` 是开始新的对话状态。

建议这样区分：

- 当前任务还没结束，但对话太长：使用 `/compact`。
- 当前任务已经结束，要开始新需求：使用 `/new`。
- 想清理当前终端显示，并以 fresh chat 继续：使用 `/clear`。

新手不用过度纠结两者差异。关键原则是：任务没结束不要随便清空上下文；任务结束后再用 `/new` 或 `/clear` 换话题。

## /diff：查看真实改动

`/diff` 用于查看文件的实际变更内容。它比口头总结更可靠，适合在任务完成后核对是否存在误改、漏改或无关重排。

检查重点：

- 是否只修改了预期文件。
- 是否存在无关格式化或大面积重排。
- 配置、命令、路径和文档链接是否正确。
- 是否遗漏测试、文档或示例更新。

## Git / commit / PR 操作边界

完整操作手册见 [Git 融入 AI 工作流](./git-workflow.md)。本节只记录核心边界。

AI 执行文件修改前，应先查看工作区状态：

```bash
git status --short
```

检查重点：

- 是否存在用户未提交改动。
- 本次任务是否会修改同一批文件。
- 是否有未跟踪文件需要保留或记录。
- 是否需要先阅读最近的 `docs/changes/` 变更记录。

默认规则：

- AI 可以查看 `git status`、`git diff` 和提交历史。
- AI 不应默认执行 commit、tag、push、rebase、reset 或清理工作区。
- AI 可以自动准备 commit message、提交摘要和待提交文件清单，但不能自动执行 commit。
- 准备 commit 时，AI 必须先汇报待提交文件、变更摘要、验证结果和建议 commit message。
- 只有用户明确回复“确认提交”“yes”“commit”后，AI 才能执行 `git add` 和 `git commit`。
- `git push` 必须单独授权，不能跟随 commit 自动执行。
- 如果发现无关改动，不要回滚；只说明它们存在，并把本次修改限制在目标文件内。

推荐提交前顺序：

```text
运行验证命令 -> 确认 docs/changes 记录 -> /diff -> /review -> AI 准备 commit -> 用户授权 commit -> 用户单独授权 push
```

AI 汇报 commit 准备信息时建议使用：

```text
准备提交以下文件：
- <path>：<改动摘要>

验证结果：
- <命令>：通过 / 失败原因

建议 commit message：
<动词 + 对象>

本次不会提交：
- <用户已有改动或无关文件>
```

提交信息建议继续使用简短中文动宾结构：

```text
补充 AI 项目清单
更新 Git 操作边界
```

PR 描述建议包含：

- 变更摘要。
- 影响文件或模块。
- 验证命令和结果。
- 已知风险和未覆盖项。
- 是否涉及配置默认值、数据迁移或发布流程变化。

## /review：提交前代码审查

`/review` 用于提交前进行一次代码审查。重点不是复述改动，而是找缺陷、风险、回归点和测试缺口。

适合检查：

- 业务逻辑是否有边界条件遗漏。
- 错误处理、并发、权限、性能是否存在风险。
- 测试是否覆盖关键路径。
- 文档与实现是否一致。

建议在 `/diff` 之后使用 `/review`。先确认真实改动，再审查风险，能减少基于错误上下文得出结论的概率。
