# LLM 与 Agent 原理地图

本文记录理解 AI Agent 系统所需的原理层知识。它不替代 [Codex Agent 运行机制](./codex-agent-runtime.md)、[AI 编程工作流](./ai-workflow.md)、[Agent 上下文管理](./context-management.md)、[Function Calling 使用笔记](./function-calling.md)、[RAG 使用笔记](./rag.md) 或 [Codex MCP 使用笔记](./mcp.md)，而是解释这些机制背后的共同基础。

## 结论

当前仓库已经覆盖较多 AI 编程实践，但还需要补一层原理地图：

```text
LLM 如何生成文本
-> 多轮对话如何被客户端维护
-> Agent 如何进入观察、决策、工具调用和继续执行的循环
-> 上下文、记忆、RAG 和工具调用如何分工
-> 为什么 Agent 会失败，以及如何验证
```

理解这条链路后，再看 [Codex Agent 运行机制](./codex-agent-runtime.md)、Function Calling、MCP、RAG 和 Agent Evaluation，会更容易判断它们分别解决什么问题。

## LLM 如何生成回答

LLM 的基本输入是上下文，基本输出是 token 序列。模型不是数据库，也不是执行器；它根据当前输入预测接下来更可能出现的 token。

关键概念：

- `token`：模型处理文本的基本单位，可能是一个字、一个词的一部分或一段符号。
- 上下文窗口：一次请求中模型能看到的最大输入和输出范围。
- 消息角色：`system`、`developer`、`user`、`assistant` 等角色会共同组成模型输入。
- 生成过程：模型逐步生成 token，每一步都受前文、指令、参数和工具结果影响。

这解释了两个常见现象：

- 模型不会天然知道当前仓库文件，除非客户端把文件内容、搜索结果或工具输出放进上下文。
- 模型回答不等于事实本身，事实仍需要来自文件、命令、日志、测试或外部工具。

## 推理、采样与模型参数

模型输出通常不是固定结果，而是由模型能力、上下文和生成参数共同决定。

常见参数：

| 参数 | 作用 |
| --- | --- |
| `temperature` | 控制输出随机性，越高越发散，越低越稳定。 |
| `top_p` | 从累计概率范围内采样，限制候选 token 分布。 |
| `max tokens` | 限制输出长度，过小可能截断结果。 |
| `reasoning effort` | 控制模型在复杂任务上投入的推理预算，不等同于输出长度。 |
| 结构化输出 | 要求模型按 schema 或固定格式输出，降低解析不确定性。 |

工程任务通常更需要稳定和可验证，而不是发散创意。对于代码、配置、文档验收和审查任务，应优先使用低随机性、明确格式和可执行验证。

## 多轮对话如何实现

多轮对话不是模型在内部永久记住了所有历史，而是客户端在后续请求中继续提供会话历史、摘要或引用标识。

常见实现方式：

```text
第 1 轮：用户消息 -> 模型回答
第 2 轮：历史消息 + 新用户消息 -> 模型回答
第 N 轮：历史消息或摘要 + 新上下文 + 新用户消息 -> 模型回答
```

两种常见形态：

- 无状态 API：每次请求都显式携带必要历史、工具调用记录和上下文。
- 有状态 API：服务端保存上一轮响应或会话状态，客户端用会话 ID 或 `previous_response_id` 继续。

可以按三层理解“有没有状态”：

| 层级 | 状态在哪里 | 说明 |
| --- | --- | --- |
| 模型层 | 模型参数和当前上下文 | 模型不会因为一次对话就永久记住你的项目；它只基于本次请求可见内容生成回答。 |
| API / 请求层 | 请求体、服务端响应状态或会话标识 | 应用可以每轮显式重放上下文，也可以用 `previous_response_id` 或会话 ID 让服务端续接。 |
| Agent runtime 层 | 客户端、本地会话文件、工具结果和任务状态 | Codex 这类 Agent 会维护当前会话、工具调用、文件观察、计划、验证结果和权限状态。 |

### API 层怎么判断有状态或无状态

讨论 API 层时，不要把“客户端本地有状态”和“模型服务端有状态”混在一起。

| API 形态 | 状态在哪里 | 判断标准 |
| --- | --- | --- |
| 无状态 API | 应用自己的数据库、内存、文件或请求体 | 每次请求都显式传入当前需要的历史消息、工具调用记录、工具输出和上下文。 |
| 有状态 API | 模型服务端保存或引用上一轮 response / conversation | 下一次请求只传新输入和 `previous_response_id`、`conversation_id` 这类状态标识，由服务端续接上下文。 |

几个容易混淆的点：

- 登录账号只代表认证身份，不等于 API 调用一定有状态。
- 本机 session 和服务端 conversation 不是同一个东西，不能默认认为两边保存了完全一致的上下文。
- 如果模型服务端不支持状态续接，客户端需要把当前需要的上下文重新发给服务端。
- 重新发送上下文通常会经过选择、裁剪、摘要或压缩，不是把所有历史无限制发送。
- 是否使用有状态 API，要看请求是否依赖服务端保存的 `previous_response_id`、`conversation_id` 或等价状态。

因此，Codex 也要分层理解：

```text
Codex 客户端层：有状态，保存和恢复本地会话、工具结果、计划和执行观察。
Codex 调服务商 API：可能有状态，也可能无状态，取决于具体实现和服务商能力。
第三方兼容服务商：不一定完整支持 previous_response_id 或 conversation。
```

做 Function Calling 应用时，默认推荐先采用无状态请求：

- 兼容 OpenAI 兼容代理更稳。
- 日志里包含完整必要上下文，问题更容易复现。
- 调试时能直接看到模型基于什么输入做判断。
- 后续更换服务商时，不容易被服务端会话实现差异卡住。

所以，“Codex 是有状态还是无状态”要分层回答：

```text
模型本身：不永久记忆当前任务。
Function Calling demo：应用层采用无状态请求，第二次请求显式回传必要上下文。
Codex CLI：作为 Agent 客户端是有状态的，会维护并恢复会话上下文。
```

Codex 的有状态主要体现在：

- 当前交互会话会持续累积用户消息、模型回答、工具调用结果和执行观察。
- `codex resume` 可以恢复之前的交互会话；`codex resume --last` 会继续最近会话。
- `codex fork` 可以基于已有会话复制一条新路线，之后新旧会话各自发展。
- `/compact` 会把长会话压缩成后续可继续使用的摘要，减少上下文过长带来的丢失和跑偏。
- 本地会话记录、配置、权限和工作目录共同影响 Agent 后续能看到什么、能做什么。

但这不等于模型自己拥有长期记忆。更准确的说法是：

```text
Codex 是有状态的 Agent 客户端；
它让模型“记住”的方式，是管理、恢复、压缩并重新提供上下文。
```

这和无状态 Function Calling demo 并不冲突。demo 选择无状态，是为了兼容 OpenAI 兼容代理并让链路更透明；Codex 作为交互式编程 Agent，需要支持长任务、多轮工具调用、跨天恢复和分支探索，因此在客户端体验上必须维护状态。

无论哪种形态，工程上都要关注：

- 历史越长，越容易超过上下文窗口。
- 摘要压缩会丢信息，必须保留目标、事实、已修改内容、验证结果和风险。
- 过期历史可能污染当前任务，需要用真实文件和命令重新校准。

## Agent 如何调用工具

普通聊天模型只生成文本。Agent 在模型外面加了一层运行时，让模型可以在多轮中选择行动、读取结果并继续判断。

典型 Agent 循环：

```text
用户目标
-> 组装上下文和可用工具说明
-> 模型判断下一步
-> 如果需要工具，生成工具名和参数
-> 客户端或运行时执行工具
-> 工具结果回到上下文
-> 模型继续计划、解释、修改或停止
```

对 Codex 来说，这个状态不只是聊天历史，还包括读过的文件、运行过的命令、失败输出、当前 diff、计划进度、用户授权和仓库规则。Agent 能连续推进任务，靠的是 runtime 把这些观察结果组织进后续上下文，而不是模型在内部自动保留完整世界状态。

对应到 ReAct：

- `Thought`：模型或运行时判断当前状态和下一步。
- `Action`：调用搜索、读文件、运行测试、MCP tool、Function Calling 等工具。
- `Observation`：工具返回真实结果。
- `Adjust`：基于结果继续、修正、缩小范围或停止。

Agent 的关键不是“模型自己会执行”，而是客户端给了它受控工具，并把工具结果重新送回模型。

## Function Calling、MCP、RAG 的关系

这些能力都在扩展模型可用的信息或行动范围，但分工不同：

| 能力 | 解决的问题 | 谁执行真实动作 |
| --- | --- | --- |
| Function Calling | 让模型生成结构化函数名和参数。 | 应用程序执行本地函数或业务接口。 |
| MCP | 用统一协议把外部工具、资源和提示模板暴露给 AI 客户端。 | MCP server 执行受控能力。 |
| RAG | 先检索外部知识，再让模型基于资料回答。 | 应用或检索服务执行检索。 |
| Shell / 本地工具 | 让 Agent 读取文件、运行测试或调用 CLI。 | Agent 运行时按权限执行命令。 |

它们可以组合。例如：

```text
用户问题
-> Agent 判断需要查项目知识
-> 通过 MCP 调用文档检索或 CodeGraph
-> 必要时用 Function Calling 查询业务数据
-> 把结果放回上下文
-> 模型生成回答或下一步计划
```

## 上下文、记忆和 RAG 的边界

这几个概念容易混用：

| 概念 | 作用 | 常见风险 |
| --- | --- | --- |
| 当前上下文 | 本次请求模型能看到的消息、文件片段、工具结果。 | 太少会猜测，太多会稀释重点。 |
| 历史消息 | 多轮对话中的原始消息记录。 | 过长、过期或混入无关任务。 |
| 摘要压缩 | 把长会话压缩成关键事实。 | 丢失边界、验证结果或失败原因。 |
| 长期记忆 | 跨会话保存偏好、项目事实或常用流程。 | 记忆过期后误导任务。 |
| RAG | 按需检索外部资料并注入上下文。 | 检索错资料会让回答看起来有依据但实际错误。 |

工程上更可靠的顺序是：

```text
先用文件、命令和工具取得事实
-> 再把事实放入上下文
-> 长任务用摘要或 docs/changes 沉淀
-> 需要外部知识时再引入 RAG 或 MCP
```

## Agent 为什么会失败

Agent 的失败通常不是单一原因，而是多个环节叠加。

常见失败来源：

- 上下文缺失：模型没有看到真实文件、日志或约束。
- 上下文污染：旧结论、无关 diff 或过期文档被当成当前事实。
- 工具描述不清：模型不知道什么时候该调用哪个工具。
- 工具结果过长：关键信息被噪声淹没。
- 权限边界模糊：Agent 执行了不该自动执行的操作。
- 验证缺失：没有测试、lint、typecheck、构建或等价检查。
- 停止条件缺失：Agent 在失败后反复尝试，无法及时交还人类。

因此，可靠的 Agent 系统需要同时具备：

- 清晰目标和边界。
- 准确上下文。
- 小而明确的工具。
- 可执行验证。
- 明确停止线。
- 可追溯的变更记录。

## 推荐学习顺序

建议按以下顺序理解本仓库内容：

1. 先读本文，建立 LLM、多轮对话、Agent 循环和工具调用的原理地图。
2. 阅读 [AI 编程工作流](./ai-workflow.md)，理解不同任务应该用 vibe、plan 还是 spec。
3. 阅读 [Agent 上下文管理](./context-management.md)，理解如何给 Agent 提供准确上下文。
4. 阅读 [Function Calling 使用笔记](./function-calling.md)，理解模型如何生成结构化函数调用。
5. 阅读 [RAG 使用笔记](./rag.md)，理解外部知识如何进入模型上下文。
6. 阅读 [Codex MCP 使用笔记](./mcp.md)，理解工具、资源和外部系统如何统一接入。
7. 阅读 [Agent 评估与验证](./agent-evaluation.md)，理解如何判断 Agent 产出是否可交付。

如果只记住一句话：

```text
LLM 负责生成，Agent runtime 负责组织上下文和执行工具，验证系统负责判断结果是否可信。
```
