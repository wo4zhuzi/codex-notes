# AI Agent 完整项目实战复盘与优化方案

本文记录一次使用 AI Agent 完整实现项目后的复盘。重点不是评价某个模型，而是总结在真实工程场景下，如何让 Agent 更稳定交付、减少返工，并在同等项目复杂度下降低无效 token 消耗。

## 项目背景

这次实践目标是验证 AI Agent 是否具备“从设计到实现再到部署”的完整项目交付能力。因此项目没有选得过于简单，也没有设计成特别复杂的企业级系统。

项目技术与业务范围如下：

- 后端使用 Golang，框架使用 go-zero，采用微服务架构。
- 前端使用 Next.js + Ant Design X。
- 数据层包含三套 MySQL 数据库，不同微服务使用不同数据库，同时使用 Redis。
- 数据库、后端和前端都使用 Docker / Dockerfile / docker-compose 部署，目标是一键启动。
- 业务功能包括注册、登录、AI Function Calling 和看板。
- 开发过程使用 Spec 模式，先设计再执行。

整体结果是：AI Agent 在工程脚手架、Docker、Dockerfile、docker-compose、后端基础代码、单元测试等规则明确的任务上表现很好，执行过程顺畅；但在产品细节、业务语义、异常提示、页面跳转、失败重试控制上暴露出明显问题。

## 现象与根因

### Docker 和基础工程能力很强

AI Agent 对 Docker、Dockerfile、docker-compose、服务编排、环境变量和启动脚本这类任务表现突出，基本可以一遍通过。

根因是这些任务有明确模板、明确输入输出和明确机械验证方式。只要项目结构清楚，Agent 可以通过构建、启动和日志快速判断结果。

### Spec 模式能显著提升执行顺畅度

前期使用 Spec 模式会多消耗一些 token，但执行阶段更流畅，阻碍更少，代码、单元测试和基础验证可以连续完成。

根因是 Spec 提前锁定了服务边界、接口契约、目录结构、部署方式和验证命令，减少了 Agent 在执行阶段反复猜测。

### 前期规划不足会放大后续 token 消耗

如果前期没有规划好，后续让 Agent 改代码时，token 消耗会明显增加。

根因是返工通常不是单点修改。一次需求补充可能牵动接口、后端错误码、前端页面、测试、文档和 Docker 环境。Agent 需要重新读取上下文、判断影响面、修改多处文件并重新验证。

### 命令卡住会造成 token 空转

例如 `npm install` 某个包下载失败时，如果没有停止规则，Agent 可能持续重试或反复解释错误，造成无效 token 消耗。

根因是 Agent 默认会尝试推进任务，但依赖下载、网络访问、镜像拉取和包管理器解析问题不一定能靠继续推理解决。没有超时、重试上限和交还规则时，Agent 很容易在外部环境问题上空转。

### 基础功能顺畅，产品细节不足

Agent 按任务执行基础功能通常很顺，例如注册、登录、接口、数据库写入和单元测试。但实际完成后，细节经常不够好，例如：

- 注册成功后没有跳转。
- 重复注册没有给出符合业务语义的提示。
- 后端返回“资源重复”，前端没有映射成“该账号已注册”。
- 表单错误、loading、重复提交、空状态和异常兜底没有被完整考虑。

根因是“功能完成”和“产品可用”不是同一个标准。如果 Spec 只写“实现注册功能”，Agent 往往会做到接口和页面能跑；但如果没有明确页面跳转、错误文案、交互状态和验收标准，它不会稳定补齐这些产品细节。

## 推荐工作流

推荐采用：

```text
项目背景说明 -> Spec -> 垂直切片 -> 验收清单 -> 失败预算 -> 最终审查
```

### 1. 项目启动前先固定工程边界

在开始实现前，先让 Agent 明确这些内容：

- 技术栈固定项：go-zero、Next.js、Ant Design X、MySQL、Redis、Docker Compose。
- 服务边界：每个微服务负责什么，不负责什么。
- 数据库归属：哪个服务使用哪套 MySQL。
- 环境变量：变量名、默认示例、是否进入 `.env.example`。
- 验证命令：后端测试、前端构建、Docker Compose 启动、关键手动流程。
- Git 边界：是否允许提交、是否允许 push、如何处理已有未提交改动。

这些内容适合写入项目 `AGENTS.md`、`docs/specs/` 或项目 README，避免每个任务重新解释。

### 2. Spec 不只写架构，也写验收细节

Spec 阶段除了写服务、接口、表结构和部署方式，还要写用户流程验收标准。

推荐把核心流程写成可检查条目：

```text
注册流程验收：
1. 用户输入合法邮箱、密码和昵称后，可以注册成功。
2. 注册成功后跳转到登录页或看板页，跳转目标必须在 Spec 中明确。
3. 重复注册时，页面提示“该账号已注册”。
4. 参数错误时，错误展示在表单附近，不能只出现在控制台。
5. 提交中按钮进入 loading 状态，不能重复提交。
6. 网络异常时，页面展示可理解的失败提示。
7. 后端返回稳定错误码，前端不要直接展示底层错误文本。
```

这类细节不要等实现完成后再补。越晚补，返工越多。

### 3. 按垂直切片执行

不要一次性让 Agent 完成所有后端、所有前端、所有部署。更推荐按业务闭环切分：

1. 基础工程与 Docker Compose。
2. 用户注册完整闭环。
3. 用户登录完整闭环。
4. AI Function Calling 完整闭环。
5. 看板核心流程。
6. 统一错误处理、文案和体验细节。
7. 最终交付审查。

每个切片都应该包含后端、前端、测试和验收，而不是只完成某一层代码。

### 4. 给失败命令设置预算

对安装、构建、测试、镜像拉取等容易卡住的命令，提前写清楚停止条件。

推荐规则：

```text
同一失败命令最多重试 2 次。
依赖安装、Docker pull、构建命令超过 5 分钟必须停止分析。
不允许无限重试 npm install、pnpm install、go mod tidy、docker pull。
失败后必须汇报：失败命令、错误摘要、已尝试方案、建议下一步。
涉及换源、升级依赖、删除 lockfile、清理缓存时必须先说明风险。
```

这样可以避免 Agent 在外部网络或依赖源问题上消耗大量 token。

### 5. 最后单独做产品细节审查

基础功能完成后，应单独发起一轮“产品细节审查”，不要只依赖实现任务中的自检。

重点检查：

- 注册成功、登录成功、退出登录后的跳转是否符合 Spec。
- 重复注册、密码错误、账号不存在、参数错误、网络异常的提示是否准确。
- loading、禁用按钮、重复提交、空状态是否完整。
- 后端错误码是否能映射成稳定的前端业务文案。
- 页面是否存在只在控制台报错、用户无感知的失败。
- Docker Compose 一键启动后，关键业务链路是否能跑通。

## Token 控制策略

同等项目条件下，减少 token 的核心不是压缩所有设计，而是减少无效消耗。

### 前期 Spec token 是值得的

前期多写一点 Spec，通常比后期反复改代码更省。尤其是微服务、数据库分库、前后端联调、Docker Compose 部署这类项目，返工成本远高于一次性把边界说清楚。

### 用清单替代长篇自然语言

Agent 更容易执行明确清单。比如“注册要体验好”不如直接写：

```text
注册成功后跳转到看板。
重复注册提示“该账号已注册”。
提交中按钮显示 loading 并禁用。
后端 duplicate_user 错误码映射为中文业务文案。
```

清单更短，也更容易验收。

### 大任务拆成短上下文

每次只给 Agent 当前切片需要的上下文：

- 当前 Spec 片段。
- 相关接口和页面。
- 相关测试。
- 当前验收清单。

不要让 Agent 每轮都重新理解整个项目。

### 明确停止条件

安装、构建、测试失败时，要求 Agent 先定位根因，再决定是否继续。连续失败时交还人类决策，比无限尝试更省 token。

## 推荐验收清单

### 功能验收

- 注册、登录、退出登录、重复注册、密码错误、账号不存在都能按预期反馈。
- AI Function Calling 能完成一次真实工具调用闭环，并能处理工具失败。
- 看板能展示有数据、无数据、加载中和失败状态。
- 刷新页面后登录态和权限状态符合预期。

### 接口与错误验收

- API 返回稳定错误码，不把数据库错误或框架错误直接暴露给前端。
- 前端根据错误码映射业务文案。
- 参数错误、业务冲突、权限失败、系统异常能区分。
- 单元测试覆盖成功路径和关键失败路径。

### 工程与部署验收

- `docker compose up` 可以启动数据库、Redis、后端和前端。
- `.env.example` 或 README 说明必要环境变量。
- 后端测试、前端构建、关键手动流程都有验证记录。
- `git diff` 只包含当前任务相关改动。

## 可复用 Agent 约束

下面这段可以放入项目 `AGENTS.md` 或任务 prompt：

```text
本任务按 Spec 执行。实现前必须先读取相关 Spec 和现有代码，定位当前状态后再给出执行计划。

执行约束：
1. 每次只完成一个垂直切片。
2. 修改前说明根因或当前判断。
3. 修改后运行对应测试、构建或手动验收命令。
4. 同一失败命令最多重试 2 次。
5. 依赖安装、网络下载、Docker pull 超过 5 分钟或连续失败时必须停止，汇报错误和建议，不得无限重试。
6. 不允许为了通过测试随意放宽业务规则。
7. 完成后必须汇报：根因、改动、验证结果、未覆盖风险。

验收重点：
- 不只验证接口成功，还要验证用户流程。
- 错误提示必须是业务语义文案，不能直接暴露底层错误。
- 注册、登录、重复注册、参数错误、网络失败、重复提交都必须覆盖。
```

## 方案取舍

### 方案 A：一次性完整委托

适合快速 demo 或能力验证。

优点是速度快，Agent 可以连续生成大量工程代码。缺点是后期产品细节和边界问题会集中暴露，返工 token 高。

### 方案 B：Spec + 垂直切片

适合完整实战项目。

优点是成功率高、验收清楚、返工少。缺点是前期设计 token 更多。对于 Go-zero 微服务、Next.js、MySQL、Redis 和 Docker Compose 这种多组件项目，这是推荐方案。

### 方案 C：Spec + TDD

适合核心业务规则、权限、支付、数据一致性和复杂边界条件。

优点是回归风险最低。缺点是测试编写成本更高。建议在注册登录、权限、关键业务规则上使用，不必全项目强制使用。

## 总结

AI Agent 已经很适合完成完整项目中的工程化工作，尤其是脚手架、Docker、服务编排、基础接口和测试。但要让它交付“产品可用”的系统，不能只给功能列表，还要给明确的业务语义、用户流程、错误文案、失败停止规则和验收清单。

更稳的实践方式是：前期用 Spec 锁定边界，中期按垂直切片执行，执行中限制失败重试，收尾时单独做产品细节审查。这样通常会比一次性委托更省总 token，也更接近生产可用结果。
