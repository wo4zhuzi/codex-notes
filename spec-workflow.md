# Spec 模式实践

本文记录 Spec 模式的落地流程，供新项目、跨模块重构、高风险功能和夜间委托任务使用。它是 [AI 编程工作流](./ai-workflow.md) 的实践手册，不替代 `AGENTS.md`。

## 结论

Spec 模式的目标是在实现前锁定三件事：

```text
边界 -> 方案 -> 进度
```

只有完整 Spec 被用户确认后，agent 才能开始实现。Spec 文档记录“做之前的约束和计划”，变更记录记录“做之后的事实和验证”。

默认文档位置：

```text
docs/specs/YYYY-MM-DD-<topic>.md
docs/changes/YYYY-MM-DD-<topic>.md
```

## 什么时候使用 Spec 模式

以下任务应优先使用 Spec 模式：

- 新项目或新模块。
- 跨模块重构。
- 数据迁移、权限、支付、安全相关改动。
- 夜间委托或需要长时间自主执行的任务。
- 需要第二天验收、跨会话恢复或交给其他 agent 接手的任务。

如果任务风险判断不清，默认从 plan 升级到 spec。

## 文档分工

Spec 模式至少涉及两类文档：

| 文档 | 作用 | 生成时机 |
| --- | --- | --- |
| `docs/specs/YYYY-MM-DD-<topic>.md` | 记录目标、边界、方案、执行计划和停止条件 | 实现前，随确认过程逐步更新 |
| `docs/changes/YYYY-MM-DD-<topic>.md` | 记录实际改了什么、验证结果和后续建议 | 实现后或阶段完成后 |

不要把执行后的细节都塞回 Spec。Spec 是合同，changes 是事实记录。

## Spec 生成流程

### 1. 目标输入

用户先给一句话目标，不要求一次说全。

示例：

```text
我要做一个本地优先的个人看板工具，用于管理 Todo / Doing / Done 三列任务。
```

Agent 应先复述目标，并判断是否需要 Spec 模式。

### 2. 边界阶段

边界阶段只确认范围，不写代码。

必须输出：

- 一句话目标。
- 做什么。
- 不做什么。
- 用户是谁。
- 成功标准。
- 风险点。
- 需要用户确认的问题。

用户确认后，写入或更新：

```text
docs/specs/YYYY-MM-DD-<topic>.md
```

### 3. 方案阶段

方案阶段只设计实现方式，不写代码。

必须输出：

- 技术栈或既有技术栈适配。
- 目录结构。
- 核心模块职责。
- 数据模型或关键类型。
- 页面、接口、命令或执行流。
- 验证方式。
- 关键取舍。

用户确认后，更新同一个 Spec 文档。

### 4. 进度阶段

进度阶段生成可执行计划。

必须输出：

- 任务拆解。
- 每步涉及的文件或模块。
- 每步验证方式。
- 失败后的自愈策略。
- 最多自愈轮数，建议 1 到 3 轮。
- 停止条件。
- 完成后生成 `docs/changes/` 记录的要求。

用户确认后，更新同一个 Spec 文档。

### 5. 授权执行

只有在用户明确授权后，agent 才能实现。

推荐授权语句：

```text
按已确认的 spec 和 plan 执行。
只实现 spec 内功能，不扩展新功能。
验证失败先定位根因，再自愈修复，最多 3 轮。
超过 3 轮仍失败就停止并记录原因。
完成后生成 docs/changes/YYYY-MM-DD-<topic>.md。
```

### 6. 第二天或跨会话验收

如果任务跨天执行，第二天进入项目后：

```bash
codex resume --last
```

恢复后先执行：

```text
/status
```

验收顺序：

1. 阅读 Spec 文档，确认目标和边界。
2. 阅读 `docs/changes/`，确认实际变更和验证结果。
3. 查看 `git status --short` 和 diff。
4. 运行项目验证命令。
5. 手动验收核心业务流程。
6. 检查是否实现了 Spec 外功能。

## Spec 文档模板

```markdown
# <项目或功能名> Spec

## 目标

## 边界

### 做什么

### 不做什么

## 用户与场景

## 成功标准

## 风险点

## 方案

## 模块 / 文件结构

## 数据流 / 执行流

## 验证方式

## 执行计划

## 停止条件

## 进度记录
```

## 可复制 Prompt

### 边界阶段

```text
我们用 spec 模式做这个项目。

项目目标：<一句话目标>

第一阶段只做边界确认，不要写代码。
请输出：
1. 一句话目标
2. 做什么
3. 不做什么
4. 用户是谁
5. 成功标准
6. 风险点
7. 需要我确认的问题

边界确认后，再写入 docs/specs/YYYY-MM-DD-<topic>.md。
```

### 方案阶段

```text
边界确认，进入方案阶段。
仍然不要写代码。

请输出：
1. 技术栈或既有技术栈适配
2. 目录结构
3. 核心模块职责
4. 数据模型或关键类型
5. 页面、接口、命令或执行流
6. 验证方式
7. 关键取舍

方案确认后，更新同一个 spec 文档。
```

### 进度阶段

```text
方案确认，进入进度阶段。

请生成可执行计划：
1. 任务拆解
2. 每步涉及哪些文件或模块
3. 每步如何验证
4. 验证失败如何自愈
5. 最多自愈 3 轮，仍失败就停止
6. 完成后如何生成 docs/changes/YYYY-MM-DD-<topic>.md

进度确认后，更新同一个 spec 文档。
```

### 夜间执行授权

```text
按已确认的 spec 和 plan 执行。

执行规则：
1. 不扩展 spec 外功能。
2. 每个阶段完成后运行对应验证。
3. 验证失败先定位根因，再自愈修复，最多 3 轮。
4. 超过 3 轮仍失败就停止并记录原因。
5. 完成后生成 docs/changes/YYYY-MM-DD-<topic>.md。
6. 最终汇总完成项、验证结果、失败项和变更文档路径。
```

### 第二天验收

```text
请基于当前仓库状态、git diff、docs/specs 和 docs/changes 汇总：
1. 昨晚完成了什么
2. 哪些验证通过
3. 哪些验证失败
4. 是否有越界实现
5. 我现在应该如何验收
```

## AGENTS.md 应该写什么

`AGENTS.md` 只写触发规则和文档位置，不写完整手册：

```text
高风险任务、新项目、跨模块重构、数据迁移、权限/支付/安全相关改动，必须先进入 Spec 模式。
Spec 文档位置：docs/specs/YYYY-MM-DD-<topic>.md。
用户确认完整 Spec 前，不得开始实现。
执行完成后，必须生成 docs/changes/YYYY-MM-DD-<topic>.md。
```
