# TDD 实践手册

本文记录 TDD 在 AI 编程工作流中的落地方式。它和 [Spec 模式实践](./spec-workflow.md) 配合使用：Spec 负责锁定需求边界，TDD 负责用测试锁定实现行为。

## 结论

TDD 不是为了追求测试覆盖率，而是用测试把需求变成可执行约束。

推荐流程：

```text
Spec -> 测试清单 -> Red -> Green -> Refactor -> 验证 -> docs/changes
```

一句话：

```text
Spec 说明做什么，TDD 证明做对了。
```

## TDD 和 Spec 的区别

| 项目 | Spec | TDD |
| --- | --- | --- |
| 核心问题 | 做什么、边界是什么 | 行为是否正确 |
| 发生时机 | 实现前 | 实现中 |
| 主要产物 | `docs/specs/YYYY-MM-DD-<topic>.md` | 测试代码 |
| 作用 | 降低需求和方案误解 | 防止实现偏差和回归 |
| 适合场景 | 新项目、大功能、高风险任务 | 业务逻辑、bugfix、重构、边界条件 |

不要用 TDD 替代 Spec。需求不清楚时，先做 Spec；边界确认后，再用 TDD 实现核心逻辑。

## 什么时候使用 TDD

优先使用 TDD：

- 核心业务规则。
- Bugfix，尤其是防止回归的问题。
- 重构但要求行为不变。
- 权限、安全、支付、数据一致性。
- API 输入输出。
- 状态流转、表单校验、算法和数据转换。

可以不做完整 TDD：

- 纯文档修改。
- UI 视觉微调。
- 一次性原型。
- 当前项目没有测试框架，且任务风险很低。

但即使不做 TDD，也必须保留验证步骤。

## 标准流程

### 1. 从 Spec 提取验收标准

先阅读 `docs/specs/YYYY-MM-DD-<topic>.md`，提取可验证行为。

示例：

```text
用户创建任务时，标题不能为空。
新任务默认状态为 todo。
未登录用户不能创建任务。
```

### 2. 生成测试清单

按以下维度拆测试：

- 正常路径。
- 边界输入。
- 错误输入。
- 权限限制。
- 状态限制。
- 历史 bug。
- 核心不变量。

### 3. Red：先写失败测试

先写测试，不先写实现。

要求：

- 测试必须对应一个明确行为。
- 测试失败原因应符合预期。
- 不要一次写太多测试后再实现。

### 4. Green：写最小实现

只写刚好让当前测试通过的实现。

要求：

- 不提前扩展 Spec 外功能。
- 不为未来需求做复杂抽象。
- 每通过一个行为，再进入下一个行为。

### 5. Refactor：在测试保护下重构

测试通过后再整理实现。

可以做：

- 改命名。
- 拆函数。
- 消除重复。
- 调整模块边界。

不可以做：

- 改变已确认行为。
- 顺手扩展新功能。
- 跳过测试直接重构。

### 6. 完整验证

完成后运行：

```text
单个测试 -> 相关测试集 -> lint/typecheck/build -> 手动验收
```

如果验证失败：

```text
失败 -> 定位根因 -> 修复 -> 再验证
```

建议最多自愈 1 到 3 轮。仍失败时停止并记录原因。

## AI 执行规则

让 AI 用 TDD 执行时，推荐明确说：

```text
请按 TDD 实现这个功能。

规则：
1. 先从 spec 或需求中提取测试清单。
2. 每次只选择一个最小行为。
3. 先写失败测试，并运行确认失败。
4. 再写最小实现让测试通过。
5. 测试通过后再重构。
6. 每轮都运行对应验证。
7. 不实现 spec 外功能。
8. 完成后生成 docs/changes/YYYY-MM-DD-<topic>.md。
```

## Bugfix TDD Prompt

```text
请用 TDD 修复这个 bug。

要求：
1. 先复现问题并定位根因。
2. 先写一个失败测试覆盖该 bug。
3. 确认测试因该 bug 失败。
4. 写最小修复让测试通过。
5. 跑相关回归测试。
6. 记录根因、修复内容和验证结果。
```

## 重构 TDD Prompt

```text
请在行为不变的前提下重构。

要求：
1. 先确认现有测试是否覆盖核心行为。
2. 如果覆盖不足，先补行为保护测试。
3. 确认测试通过后再重构。
4. 重构过程中不改变外部接口和业务行为。
5. 重构后运行完整验证。
```

## 夜间委托 TDD Prompt

```text
请按已确认的 spec 使用 TDD 执行。

执行规则：
1. 先把 spec 的验收标准转成测试清单。
2. 每次只实现一个最小行为。
3. 每个行为都按 Red -> Green -> Refactor 推进。
4. 验证失败先定位根因，再自愈修复，最多 3 轮。
5. 不扩展 spec 外功能。
6. 如果测试框架缺失或无法运行，停止并说明原因，不要伪造测试结果。
7. 完成后生成 docs/changes/YYYY-MM-DD-<topic>.md。
```

## 无测试框架项目怎么办

如果项目没有测试框架，不要为了低风险任务强行引入复杂测试体系。

推荐处理：

- 记录手动验证步骤。
- 对核心逻辑优先抽出可测试函数。
- 新项目在 Spec 阶段就确定测试框架。
- 高风险任务没有测试框架时，应先补最小测试能力。

无测试框架时，变更记录必须写清楚：

```text
本次未使用自动化测试。
原因：<原因>
替代验证：<手动命令或检查步骤>
残余风险：<风险>
```

## 和 docs/changes 的关系

TDD 执行完成后，如果产生仓库改动，必须生成或更新：

```text
docs/changes/YYYY-MM-DD-<topic>.md
```

变更记录至少包含：

- 任务背景。
- 根因定位。
- 测试清单。
- 新增或修改的测试。
- 实现内容。
- 验证命令和结果。
- 未覆盖风险。
