# 新增 Codex MCP 使用笔记

## 任务背景

用户希望补充一个 MCP 章节。MCP 内容较多，包含原理、使用和设计思路；命令部分需要在 `codex-core-commands.md` 中体现，具体内容单独写一个 Markdown 文件。

## 根因定位

仓库此前没有 MCP 相关内容。`codex-core-commands.md` 当前定位是核心指令速查和协作流程说明，不适合承载 MCP 的长篇原理、配置、认证、设计和排查内容。仓库已有 `codex-skills.md` 这类专题文档，因此新增独立 `mcp.md` 更符合现有信息架构。

本机命令核验结果显示 Codex CLI 当前支持：

- `codex mcp list/get/add/remove/login/logout`
- `codex mcp-server`
- stdio MCP server：`codex mcp add <name> -- <command> [args...]`
- Streamable HTTP MCP server：`codex mcp add <name> --url <url>`
- bearer token 环境变量：`--bearer-token-env-var <ENV_VAR>`
- OAuth scopes：`codex mcp login <name> --scopes scope-a,scope-b`

## 执行计划

1. 查看 `git status --short`，确认工作区没有用户已有未提交改动。
2. 阅读 `README.md`、`codex-core-commands.md`、`codex-skills.md` 和已有变更记录，确认文档结构和写法。
3. 用本机 `codex --help`、`codex mcp --help` 和子命令 `--help` 核验 MCP 命令事实。
4. 查阅 MCP 官方文档，核验 `stdio`、`Streamable HTTP`、tools/resources/prompts 和安全提示。
5. 新增 `mcp.md`，按“使用优先”组织 MCP 原理、命令、认证、设计、安全和排查内容。
6. 更新 `codex-core-commands.md`，只加入 MCP 命令速查和跳转链接。
7. 更新 `README.md`，补充 `mcp.md` 的目录入口和阅读建议。
8. 新增本变更文档，记录背景、根因、计划、变更、验证和后续建议。
9. 执行本地检查，确认链接、敏感信息和 diff 范围。

AI 自检结论：计划包含根因定位、修改步骤、验证方式和风险边界；本次只修改文档，不涉及应用源码、配置默认值、依赖安装、自动化测试或提交操作。

## 变更内容

- 新增 `mcp.md`：
  - 说明 MCP 的 client、server、tool、resource 和 prompt 基本关系。
  - 记录 Codex MCP 管理命令、stdio server、Streamable HTTP server、OAuth 和 bearer token 使用方式。
  - 补充 MCP server 设计思路，包括工具边界、权限边界、失败处理和上下文控制。
  - 补充安全注意事项和排查清单。
- 更新 `codex-core-commands.md`：
  - 新增 `MCP：管理外部工具服务` 章节。
  - 记录 MCP 常用命令和使用边界。
  - 链接到 `mcp.md`。
- 更新 `README.md`：
  - 在内容说明、目录结构、快速入口和使用建议中加入 `mcp.md`。

## 验证结果

- 已执行 `git status --short`：显示本次修改 `README.md`、`codex-core-commands.md`，新增 `mcp.md` 和本变更文档；未发现其他无关改动。
- 已执行 `rg -n "MCP|mcp" README.md codex-core-commands.md mcp.md`：确认 `README.md`、`codex-core-commands.md` 和 `mcp.md` 均包含预期入口、命令和专题内容。
- 已执行 `rg -n "TODO|FIXME|your-api-key|sk-" .`：仅命中文档中的检查命令示例，以及 `git-workflow.md` 中的示例分支名 `project-task-branch`，未发现真实密钥或遗留调试标记。
- 已执行 `git diff -- README.md codex-core-commands.md mcp.md docs/changes/2026-05-23-mcp.md`：确认已跟踪文件只包含 README 入口和核心指令 MCP 章节；新增文件内容需通过文件读取或提交后 diff 查看。
- 已查阅 MCP 官方文档：
  - `https://modelcontextprotocol.io/docs/concepts/transports`：确认标准传输包括 `stdio` 和 `Streamable HTTP`，并确认 `stdio` 日志应写 `stderr`、`stdout` 只写 MCP 消息。
  - `https://modelcontextprotocol.io/docs/concepts/tools`：确认 tools 用于让模型调用外部系统，并包含工具 schema、调用和安全提示。
  - `https://modelcontextprotocol.io/docs/concepts/resources`、`https://modelcontextprotocol.io/docs/concepts/prompts`：确认 resources 和 prompts 属于 MCP server features。
- 已执行 `command -v markdownlint`：本机未安装 `markdownlint`，因此未执行 Markdown lint。

## 后续建议

- 如果后续需要覆盖 Claude Code MCP，可另开章节或单独新增对照文档，避免混淆 Codex 命令。
- 如果后续要实际开发 MCP server，可单独补充 `mcp-server-design.md` 或示例项目，本文保持使用优先。
